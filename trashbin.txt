import React from "react";
import { Link } from "react-router-dom";
import Lottie from "lottie-react";
import Robot from "../Robot1.json";
import "./HeroStyles.css"; // Import the CSS file
import videobutton from "../videobutton.json";

const Hero = () => {
  return (
    <div className="hero">
      <div className="text-container">
        <h1 className="hero-text">Robotics And Automation Center</h1>
        <p className="subtext">A team of tech-loving minds</p>
      </div>
      <div className="content-container">
        <div className="robot">
          <Lottie animationData={Robot} />
        </div>
        <div className="vdobutton">
          <Lottie animationData={videobutton} />
        </div>
      </div>
    </div>
  );
};

export default Hero;

 
/* HeroStyles.css */
.hero {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  height: 100vh; /* Ensure the component takes the full viewport height */
  background: url("../assets/bgf.jpg") center/cover no-repeat;
}

.text-container {
  text-align: center;
  margin-top: 20px;
}

.hero-text {
  font-size: 2.5rem;
  color: white;
}

.subtext {
  font-size: 1rem;
  color: white;
}

.content-container {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  flex-grow: 1; /* Expand this section to take available space */
  position: relative; /* To make positioning of robot relative to this container */
}

.robot {
  position: absolute;
  bottom: 0;
  right: 0;
  max-width: 50%; /* Reduce the maximum width of the robot animation */
}

.vdobutton {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Make the component responsive for small screens */
@media (max-width: 768px) {
  .hero {
    background-size: cover; /* Adjust background size to cover the entire screen */
  }

  .hero-text {
    font-size: 2rem; /* Decrease font size for smaller screens */
  }

  .subtext {
    font-size: 0.8rem; /* Decrease font size for smaller screens */
  }

  .robot {
    max-width: 30%; /* Further reduce robot size on small screens */
    bottom: auto; /* Reset robot position to its default */
    top: 0; /* Position robot at the top for small screens */
  }

  .vdobutton {
    order: -1; /* Move the video button above the text content */
    margin-top: 10px; /* Add less space between the video button and text */
    max-width: 35%; /* Increase the size of the video button even more */
  }
}

 
/* HeroStyles.css */
.hero {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  height: 100vh; /* Ensure the component takes the full viewport height */
  background: url("../assets/bgf.jpg") center/cover no-repeat;
}

.text-container {
  text-align: center;
  margin-top: 20px;
}

.hero-text {
  font-size: 2.5rem;
  color: white;
}

.subtext {
  font-size: 1rem;
  color: white;
}

.content-container {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  flex-grow: 1; /* Expand this section to take available space */
  position: relative; /* To make positioning of robot relative to this container */
}

.robot {
  position: absolute;
  bottom: 0;
  right: 0;
  max-width: 50%; /* Reduce the maximum width of the robot animation */
}

.vdobutton {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Make the component responsive for small screens */
@media (max-width: 768px) {
  .hero {
    background-size: cover; /* Adjust background size to cover the entire screen */
  }

  .hero-text {
    font-size: 2rem; /* Decrease font size for smaller screens */
  }

  .subtext {
    font-size: 0.8rem; /* Decrease font size for smaller screens */
  }

  .robot {
    max-width: 30%; /* Further reduce robot size on small screens */
    bottom: auto; /* Reset robot position to its default */
    top: 0; /* Position robot at the top for small screens */
  }

  .vdobutton {
    order: -1; /* Move the video button above the text content */
    margin-top: 10px; /* Add less space between the video button and text */
    max-width: 25%; /* Increase the size of the video button */
  }
}

 
/* HeroStyles.css */
.hero {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  height: 100vh; /* Ensure the component takes the full viewport height */
  background: url("../assets/bgf.jpg") center/cover no-repeat;
}

.text-container {
  text-align: center;
  margin-top: 20px;
}

.hero-text {
  font-size: 2.5rem;
  color: white;
}

.subtext {
  font-size: 1rem;
  color: white;
}

.content-container {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  flex-grow: 1; /* Expand this section to take available space */
  position: relative; /* To make positioning of robot relative to this container */
}

.robot {
  position: absolute;
  bottom: 0;
  right: 0;
  max-width: 50%; /* Reduce the maximum width of the robot animation */
}

.vdobutton {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Make the component responsive for small screens */
@media (max-width: 768px) {
  .hero {
    background-size: cover; /* Adjust background size to cover the entire screen */
  }

  .hero-text {
    font-size: 2rem; /* Decrease font size for smaller screens */
  }

  .subtext {
    font-size: 0.8rem; /* Decrease font size for smaller screens */
  }

  .robot {
    max-width: 30%; /* Further reduce robot size on small screens */
    bottom: auto; /* Reset robot position to its default */
    top: 0; /* Position robot at the top for small screens */
  }

  .vdobutton {
    order: -1; /* Move the video button above the text content */
    margin-top: 20px; /* Add some space between the video button and text */
  }
}

 
/* HeroStyles.css */
.hero {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  height: 100vh; /* Ensure the component takes the full viewport height */
  background: url("../assets/bgf.jpg") center/cover no-repeat;
}

.text-container {
  text-align: center;
  margin-top: 20px;
}

.hero-text {
  font-size: 2.5rem;
  color: white;
}

.subtext {
  font-size: 1rem;
  color: white;
}

.content-container {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  flex-grow: 1; /* Expand this section to take available space */
  position: relative; /* To make positioning of robot relative to this container */
}

.robot {
  position: absolute;
  bottom: 0;
  right: 0;
  max-width: 50%; /* Reduce the maximum width of the robot animation */
}

.vdobutton {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Make the component responsive for small screens */
@media (max-width: 768px) {
  .hero-text {
    font-size: 2rem; /* Decrease font size for smaller screens */
  }

  .subtext {
    font-size: 0.8rem; /* Decrease font size for smaller screens */
  }

  .robot {
    max-width: 30%; /* Further reduce robot size on small screens */
  }
}

 
/* HeroStyles.css */
.hero {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  height: 100vh; /* Ensure the component takes the full viewport height */
  background: url("../assets/bgf.jpg") center/cover no-repeat;
}

.text-container {
  text-align: center;
  margin-top: 20px;
}

.hero-text {
  font-size: 2.5rem;
  color: white;
}

.subtext {
  font-size: 1rem;
  color: white;
}

.content-container {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  flex-grow: 1; /* Expand this section to take available space */
  position: relative; /* To make positioning of robot relative to this container */
}

.robot {
  position: absolute;
  bottom: 0;
  right: 0;
  max-width: 50%; /* Reduce the maximum width of the robot animation */
}

.vdobutton {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Make the component responsive for small screens */
@media (max-width: 768px) {
  .hero-text {
    font-size: 2rem; /* Decrease font size for smaller screens */
  }

  .subtext {
    font-size: 0.8rem; /* Decrease font size for smaller screens */
  }

  .robot {
    max-width: 30%; /* Further reduce robot size on small screens */
  }
}

 
/* HeroStyles.css */
.hero {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  height: 100vh; /* Ensure the component takes the full viewport height */
  background: url("../assets/bgf.jpg") center/cover no-repeat;
}

.text-container {
  text-align: center;
  margin-top: 20px;
}

.hero-text {
  font-size: 2.5rem;
  color: white;
}

.subtext {
  font-size: 1rem;
  color: white;
}

.content-container {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  flex-grow: 1; /* Expand this section to take available space */
}

.robot {
  position: absolute;
  bottom: 0;
  right: 0;
}

.vdobutton {
  display: flex;
  justify-content: center;
  align-items: center;
  
}

 
/* HeroStyles.css */
.hero {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  height: 100vh; /* Ensure the component takes the full viewport height */
  background: url("../assets/bgf.jpg") center/cover no-repeat;
}

.text-container {
  text-align: center;
  margin-top: 20px;
}

.hero-text {
  font-size: 2.5rem;
  color: white;
}

.subtext {
  font-size: 1rem;
  color: white;
}

.content-container {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  flex-grow: 1; /* Expand this section to take available space */
}

.robot {
  position: absolute;
  bottom: 0;
  right: 0;
}

.vdobutton {
  display: flex;
  justify-content: center;
  align-items: center;
  
}

 
/* HeroStyles.css */
.hero {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  height: 100vh; /* Ensure the component takes the full viewport height */
  background: url("../assets/bgf.jpg") center/cover no-repeat;
}

.text-container {
  text-align: center;
  margin-top: 20px;
}

.hero-text {
  font-size: 2.5rem;
  color: white;
}

.subtext {
  font-size: 1rem;
  color: white;
}

.content-container {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  flex-grow: 1; /* Expand this section to take available space */
}

.robot {
  position: absolute;
  bottom: 0;
  right: 0;
}

.vdobutton {
  display: flex;
  justify-content: center;
  align-items: center;
  
}

 
/* HeroStyles.css */
.hero {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  height: 100vh; /* Ensure the component takes the full viewport height */
  background: url("../assets/bgf.jpg") center/cover no-repeat;
}

.text-container {
  text-align: center;
  margin-top: 20px;
}

.hero-text {
  font-size: 2.5rem;
  color: white;
}

.subtext {
  font-size: 1rem;
  color: white;
}

.content-container {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  flex-grow: 1; /* Expand this section to take available space */
}

.robot {
  position: absolute;
  bottom: 0;
  right: 0;
}

.vdobutton {
  display: flex;
  justify-content: center;
  align-items: center;
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 10%;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  top: 50%; /* Vertically center the container */
  left: 50%; /* Horizontally center the container */
  transform: translate(-50%, -50%);
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 1rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: 1rem;
}

@media (max-width: 768px) {
  .text-container {
    top: 5%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 5%; /* Adjusted to place text slightly higher */
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 0.5rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
  margin-top: 1rem; /* Adjusted to bring it a bit higher */
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: -1.5rem; /* Moved the robot slightly higher */
}

@media (max-width: 768px) {
  .text-container {
    top: 3%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 10%;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  top: 50%; /* Vertically center the container */
  left: 50%; /* Horizontally center the container */
  transform: translate(-50%, -50%);
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 1rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: 1rem;
}

@media (max-width: 768px) {
  .text-container {
    top: 5%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 2%;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 0.5rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
  margin-top: -2.5rem; /* Adjusted to bring it further above */
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: 1rem;
}

@media (max-width: 768px) {
  .text-container {
    top: 3%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 2%;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 0.5rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
  margin-top: 1rem; /* Move the video button down */
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: -1.5rem; /* Move the robot animation up */
}

@media (max-width: 768px) {
  .text-container {
    top: 3%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 2%;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 0.5rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
  margin-top: -2rem; /* Adjusted to bring it above */
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: 1rem; /* Adjusted to move it below */
}

@media (max-width: 768px) {
  .text-container {
    top: 3%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 2%;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 0.5rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
  margin-top: 1rem; /* Moved the vdo button lower */
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: -2rem; /* Adjusted to bring the robot higher */
}

@media (max-width: 768px) {
  .text-container {
    top: 3%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 2%;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 0.5rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
  margin-top: -2rem; /* Adjusted to bring it a bit higher */
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: 1rem; /* Moved the robot slightly lower */
}

@media (max-width: 768px) {
  .text-container {
    top: 3%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 2%;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 0.5rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
  margin-top: -2rem; /* Adjusted to bring it a bit higher */
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: 0.5rem;
}

@media (max-width: 768px) {
  .text-container {
    top: 3%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 2%; /* Adjusted to place text a bit higher */
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
  font-family:san-serif;
}

.subtext {
  font-size: 1.5rem;
  font-family:san-serif;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 0.5rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
  margin-top: -1.5rem;
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: 0.5rem;
}

@media (max-width: 768px) {
  .text-container {
    top: 3%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 5%; /* Adjusted to place text slightly higher */
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 0.5rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
  margin-top: -1.5rem;
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: 0.5rem;
}

@media (max-width: 768px) {
  .text-container {
    top: 3%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 10%;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 0.5rem 0; /* Adjusted margin for vdo button */
}

.vdobutton {
  width: 80%;
  max-width: 230px;
  margin-top: -1.5rem; /* Moved the vdo button slightly above */
}

.robot {
  width: 30%;
  max-width: 250px;
  margin-top: 0.5rem; /* Moved the robot slightly down */
}

@media (max-width: 768px) {
  .text-container {
    top: 5%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 10%; /* Adjusted to create more space from the top */
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem 1rem; /* Adjusted padding to provide more space */
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 1rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
  margin-top: -1rem; /* Adjusted to bring it a bit above */
}

.robot {
  width: 30%;
  max-width: 250px;
}

@media (max-width: 768px) {
  .text-container {
    top: 5%; /* Adjusted for smaller screens */
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 1vh;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem; /*changed from 2.5 to 3 */
  margin-bottom: 0.5rem;
  font-family:san-serif;
}

.subtext {
  font-size: 1.5rem;
  font-family:san-serif;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
  position: absolute;
  top: 50%; /* Vertically center the container */
  left: 50%; /* Horizontally center the container */
  transform: translate(-50%, -50%);
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 1rem 0;
}

.vdobutton {
  width: 80%;
  max-width:230px;
  
}

.robot {
  margin-right: 1rem;
  width: 30%;
  max-width:250px;
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.75rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .text-container {
    top: 3vh;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 2rem;
  max-width: 800px;
  width: 90%;
  font-family: sans-serif;
  position: absolute;
  top: 8%;
  left: 50%;
  transform: translateX(-50%);
  z-index: 1;
}

.hero-text {
  font-size: 3.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 2rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 0;
  left: 0;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 1rem 0;
}

.vdobutton {
  width: 80%;
  max-width: 230px;
}

.robot {
  width: 30%;
  max-width: 250px;
}

@media (max-width: 768px) {
  .text-container {
    top: 10%;
    padding: 1rem;
  }

  .hero-text {
    font-size: 2.5rem;
  }

  .subtext {
    font-size: 1.25rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }

  .content-container {
    position: static;
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 1vh;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem; /*changed from 2.5 to 3 */
  margin-bottom: 0.5rem;
  font-family:san-serif;
}

.subtext {
  font-size: 1.5rem;
  font-family:san-serif;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
  position: absolute;
  top: 50%; /* Vertically center the container */
  left: 50%; /* Horizontally center the container */
  transform: translate(-50%, -50%);
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 1rem 0;
}

.vdobutton {
  width: 80%;
  max-width:230px;
  
}

.robot {
  margin-right: 1rem;
  width: 30%;
  max-width:250px;
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.75rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .text-container {
    top: 3vh;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 1vh;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem; /*changed from 2.5 to 3 */
  margin-bottom: 0.5rem;
  font-family:san-serif;
}

.subtext {
  font-size: 1.5rem;
  font-family:san-serif;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
  position: absolute;
  top: 50%; /* Vertically center the container */
  left: 50%; /* Horizontally center the container */
  transform: translate(-50%, -50%);
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 1rem 0;
}

.vdobutton {
  width: 80%;
  max-width:230px;
  
}

.robot {
  margin-right: 1rem;
  width: 30%;
  max-width:250px;
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.75rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .text-container {
    top: 3vh;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 1vh;
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 3.5rem; /*changed from 2.5 to 3 */
  margin-bottom: 0.5rem;
  font-family:san-serif;
}

.subtext {
  font-size: 1.5rem;
  font-family:san-serif;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
  position: absolute;
  top: 50%; /* Vertically center the container */
  left: 50%; /* Horizontally center the container */
  transform: translate(-50%, -50%);
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 200px;
  margin: 1rem 0;
}

.vdobutton {
  width: 80%;
  max-width:230px;
  
}

.robot {
  margin-right: 1rem;
  width: 30%;
  max-width:250px;
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.75rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .text-container {
    top: 3vh;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 0.5rem 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 1vh; /* Adjust the value for vertical position */
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.25rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 5vh;
  right: 5vw;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 250px;
  margin: 1rem 0;
}

.vdobutton {
  width: 80%;
}

.robot {
  margin-right: 1rem;
  width: 30%;
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.75rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .text-container {
    top: 3vh;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 1rem 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 2vh; /* Adjust the value for vertical position */
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.25rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
  position: absolute;
  bottom: 5vh; /* Adjust the value for vertical position */
  right: 5vw; /* Adjust the value for horizontal position */
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 250px;
  margin: 1rem 0;
}

.vdobutton {
  width: 80%;
}

.robot {
  margin-right: 1rem;
  width: 30%;
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.75rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .text-container {
    top: 5vh;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 1rem 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 5vh; /* Adjust the value for vertical position */
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.25rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%;
  max-width: 250px;
  margin: 1rem 0;
}

.vdobutton {
  width: 80%;
}

.robot {
  margin-right: 1rem;
  width: 30%;
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.75rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .text-container {
    top: 10vh;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 1rem 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 10vh; /* Adjust the value for vertical position */
  left: 50%;
  transform: translateX(-50%);
}

.hero-text {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.25rem;
  margin-bottom:2rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
  position: absolute;
  top: 50%; /* Vertically align the container */
  left: 50%;
  transform: translate(-50%, -50%);
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 60%; /* Increase the width for the video button */
  max-width: 250px; /* Adjust max width as needed */
  margin: 1rem 0;
}

.vdobutton {
  width: 80%; /* Adjust the width for smaller screens */
}

.robot {
  margin-right: 1rem; /* Add spacing from the right edge */
  width: 30%; /* Adjust the width for larger screens */
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.75rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .text-container {
    top: 20vh;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 1rem 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

.hero-text {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.25rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 40%;
  max-width: 200px;
  margin: 0.5rem 0;
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.75rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .text-container {
    margin-top: 15vh;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 1rem 0;
  }

  .content-container {
    padding: 1rem;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%); /* Center text container */
}

.hero-text {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.25rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  margin-top: 2rem;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 40%;
  max-width: 300px; /* Adjust the max width as needed */
  margin: 0.5rem 0;
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.75rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .text-container {
    margin-top: 15vh;
  }

  .content-container {
    padding: 2rem;
  }

  .vdobutton,
  .robot {
    width: 80%;
    margin: 1rem 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden;
}

.text-container {
  text-align: center;
  color: white;
  margin-top: 20vh; /* Adjust the value for vertical alignment */
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  font-family: sans-serif; /* Apply san serif font */
}

.hero-text {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.25rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 2rem;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 40%; /* Adjust width as needed */
}

.robot {
  margin-right: 1rem; /* Add some spacing from the right edge */
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.75rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .text-container {
    margin-top: 15vh;
  }

  .content-container {
    flex-direction: column;
    align-items: center;
  }

  .vdobutton,
  .robot {
    width: 80%; /* Adjust width for smaller screens */
    margin: 1rem 0;
  }

  .robot {
    margin-right: 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden; /* Prevent content from overflowing and creating scrollbars */
}

.text-container {
  text-align: center;
  color: white;
  margin: 2rem;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
}

.hero-text {
  font-size: 2rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 2rem;
  width: 100%;
  position: absolute;
  top: 0;
  left: 0;
  padding: 0 1rem;
  box-sizing: border-box;
  flex-wrap: wrap; /* Allow elements to wrap on smaller screens */
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 40%; /* Adjust width as needed */
}

.robot {
  align-self: flex-end; /* Push the robot to the bottom right */
  width: 30%; /* Adjust width as needed */
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 0.75rem;
  }

  .content-container {
    flex-direction: column;
    align-items: center;
  }

  .vdobutton,
  .robot {
    width: 80%; /* Adjust width for smaller screens */
    margin: 1rem 0;
  }

  .robot {
    align-self: center; /* Center the robot on small screens */
    margin-right: 0; /* Remove margin for centering */
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden; /* Prevent content from overflowing and creating scrollbars */
}

.text-container {
  text-align: center;
  color: white;
  margin: 2rem;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
  position: absolute;
  top: 50%; /* Vertically align text container */
  transform: translateY(-50%); /* Center text container */
}

.hero-text {
  font-size: 2rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1rem;
}

.content-container {
  display: flex;
  align-items: flex-start; /* Align content at the top */
  justify-content: center;
  margin-top: 2rem;
  width: 100%;
  position: absolute;
  bottom: 0;
  left: 0;
  padding: 0 1rem;
  box-sizing: border-box;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  max-width: 40%; /* Adjust width as needed */
}

.robot {
  align-self: flex-end; /* Push the robot to the right corner */
  margin-right: 1rem; /* Add some spacing from the right edge */
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 0.75rem;
  }

  .content-container {
    flex-direction: column;
    align-items: center;
  }

  .vdobutton,
  .robot {
    width: 80%; /* Adjust width for smaller screens */
    margin: 1rem 0;
  }

  .robot {
    align-self: center; /* Center the robot on small screens */
    margin-right: 0; /* Remove margin for centering */
  }
}

@media (min-width: 769px) {
  .hero-text {
    font-size: 3rem;
  }

  .subtext {
    font-size: 1.5rem;
  }

  .content-container {
    flex-direction: row;
    justify-content: space-between;
  }

  .vdobutton,
  .robot {
    width: 30%; /* Adjust width for larger screens */
    margin: 0;
  }

  .robot {
    align-self: flex-end;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden; /* Prevent content from overflowing and creating scrollbars */
}

.text-container {
  text-align: center;
  color: white;
  margin: 2rem;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
}

.hero-text {
  font-size: 2rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1rem;
}

.content-container {
  display: flex;
  align-items: flex-start; /* Align content at the top */
  justify-content: center;
  margin-top: 2rem;
  width: 100%;
  position: absolute;
  bottom: 0;
  left: 0;
  padding: 0 1rem;
  box-sizing: border-box;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  max-width: 40%; /* Adjust width as needed */
}

.robot {
  align-self: flex-end; /* Push the robot to the right corner */
  margin-right: 1rem; /* Add some spacing from the right edge */
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 0.75rem;
  }

  .content-container {
    flex-direction: column;
    align-items: center;
  }

  .vdobutton,
  .robot {
    width: 80%; /* Adjust width for smaller screens */
    margin: 1rem 0;
  }

  .robot {
    align-self: center; /* Center the robot on small screens */
    margin-right: 0; /* Remove margin for centering */
  }
}

@media (min-width: 769px) {
  .hero-text {
    font-size: 3rem;
  }

  .subtext {
    font-size: 1.5rem;
  }

  .content-container {
    flex-direction: row;
    justify-content: space-between;
  }

  .vdobutton,
  .robot {
    width: 30%; /* Adjust width for larger screens */
    margin: 0;
  }

  .robot {
    align-self: flex-end;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden; /* Prevent content from overflowing and creating scrollbars */
}

.text-container {
  text-align: center;
  color: white;
  margin: 2rem;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
}

.hero-text {
  font-size: 2rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 2rem;
  width: 100%;
  position: absolute;
  top: 0;
  left: 0;
  padding: 0 1rem;
  box-sizing: border-box;
  flex-wrap: wrap; /* Allow elements to wrap on smaller screens */
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  width: 40%; /* Adjust width as needed */
}

.robot {
  align-self: flex-end; /* Push the robot to the bottom right */
  width: 30%; /* Adjust width as needed */
}

@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 0.75rem;
  }

  .content-container {
    flex-direction: column;
    align-items: center;
  }

  .vdobutton,
  .robot {
    width: 80%; /* Adjust width for smaller screens */
    margin: 1rem 0;
  }

  .robot {
    align-self: center; /* Center the robot on small screens */
    margin-right: 0; /* Remove margin for centering */
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden; /* Prevent content from overflowing and creating scrollbars */
}

.text-container {
  text-align: center;
  color: white;
  margin: 2rem;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
}

.hero-text {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.25rem;
}

.content-container {
  display: flex;
  align-items: flex-start; /* Align content at the top */
  justify-content: center;
  margin-top: 2rem;
  width: 100%;
  position: absolute;
  bottom: 0;
  left: 0;
  padding: 0 1rem;
  box-sizing: border-box;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  max-width: 40%; /* Adjust the max-width as needed */
}

.robot {
  align-self: flex-end; /* Push the robot to the right corner */
  margin-right: 1rem; /* Add some spacing from the right edge */
}

@media (max-width: 768px) {
  .content-container {
    flex-direction: column;
    align-items: center;
  }
  
  .vdobutton,
  .robot {
    max-width: 80%; /* Adjust the max-width for smaller screens */
    margin: 1rem 0;
  }

  .robot {
    align-self: center; /* Center the robot on small screens */
    margin-right: 0; /* Remove the margin for centering */
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden; /* Prevent content from overflowing and creating scrollbars */
}

.text-container {
  text-align: center;
  color: white;
  margin: 2rem;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
}

.hero-text {
  font-size: 3rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  margin-top: 2rem;
  width: 100%;
  position: absolute;
  bottom: 0;
  left: 0;
  padding: 0 1rem;
  box-sizing: border-box;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  max-width: 48%; /* Adjust the max-width as needed */
}

.robot {
  align-self: flex-end; /* Push the robot to the right corner */
  margin-right: 1rem; /* Add some spacing from the right edge */
}

@media (max-width: 768px) {
  .content-container {
    flex-direction: column;
    align-items: center;
  }
  
  .vdobutton,
  .robot {
    max-width: 100%;
    margin: 1rem 0;
  }

  .robot {
    align-self: center; /* Center the robot on small screens */
    margin-right: 0; /* Remove the margin for centering */
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start; /* Adjust to bring content more above */
  height: 100vh;
  width: 100vw;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
}

.text-container {
  text-align: center;
  color: white;
  margin: 1rem;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
}

.hero-text {
  font-size: 3rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 2rem;
  width: 100%;
  padding: 0 1rem;
  box-sizing: border-box;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  max-width: 48%; /* Adjust the max-width as needed */
}

.robot {
  align-self: flex-end; /* Push the robot to the right corner */
}

@media (max-width: 768px) {
  .content-container {
    flex-direction: column;
    align-items: center;
  }
  
  .vdobutton,
  .robot {
    max-width: 100%;
    margin: 1rem 0;
  }
}

 
import React from "react";
import { Link } from "react-router-dom";
import Lottie from "lottie-react";
import Robot from "../Robot1.json";
import "./HeroStyles.css"; // Import the CSS file
import videobutton from "../videobutton.json";

const Hero = () => {
  return (
    <div className="hero">
      <div className="text-container">
        <h1 className="hero-text">Robotics And Automation Center</h1>
        <p className="subtext">A team of tech-loving minds</p>
      </div>
      <div className="vdobutton">
        <Lottie animationData={videobutton} />
      </div>
      <div className="robot">
        <Lottie animationData={Robot} />
      </div>
    </div>
  );
};

export default Hero;

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden; /* Prevent content from overflowing and creating scrollbars */
}

.text-container {
  text-align: center;
  color: white;
  padding: 1rem;
  max-width: 800px;
  width: 90%;
  box-sizing: border-box;
}

.hero-text {
  font-size: 3rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 2rem;
  width: 100%;
  position: absolute;
  bottom: 0;
  left: 0;
  padding: 0 1rem;
  box-sizing: border-box;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  max-width: 48%; /* Adjust the max-width as needed */
}

.robot {
  align-self: flex-end; /* Push the robot to the right corner */
}

@media (max-width: 768px) {
  .content-container {
    flex-direction: column;
    align-items: center;
  }
  
  .vdobutton,
  .robot {
    max-width: 100%;
    margin: 1rem 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
  overflow: hidden; /* Prevent content from overflowing and creating scrollbars */
}

.text-container {
  text-align: center;
  color: white;
  margin: 2rem;
  padding: 1rem;
}

.hero-text {
  font-size: 3rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.content-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 2rem;
  width: 100%;
  position: absolute;
  bottom: 0;
  left: 0;
  padding: 0 1rem;
  box-sizing: border-box;
}

.vdobutton,
.robot {
  animation: fadeInUp 1s ease-in-out;
  max-width: 48%; /* Adjust the max-width as needed */
}

.robot {
  align-self: flex-end; /* Push the robot to the right corner */
}

@media (max-width: 768px) {
  .content-container {
    flex-direction: column;
    align-items: center;
  }
  
  .vdobutton,
  .robot {
    max-width: 100%;
    margin: 1rem 0;
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
}

.text-container {
  text-align: center;
  color: white;
  margin-bottom: 2rem;
}

.hero-text {
  font-size: 3rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.vdobutton {
  margin-top: 2rem;
  animation: fadeInUp 1s ease-in-out;
}

.robot {
  width: 80%; /* Adjust the width as needed */
  max-width: 400px; /* Set a maximum width for responsiveness */
  margin-top: 2rem;
  animation: fadeInUp 1s ease-in-out;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
}

.text-container {
  text-align: center;
  color: white;
  margin-bottom: 2rem;
}

.hero-text {
  font-size: 3rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.vdobutton {
  margin-top: 2rem;
  animation: fadeInUp 1s ease-in-out;
}

.robot {
  width: 80%; /* Adjust the width as needed */
  max-width: 400px; /* Set a maximum width for responsiveness */
  margin-top: 2rem;
  animation: fadeInUp 1s ease-in-out;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

 
/* HeroStyles.css */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: url('../assets/bgf.jpg') no-repeat center center fixed;
  background-size: cover;
}

.text-container {
  text-align: center;
  color: white;
  margin-bottom: 2rem;
}

.hero-text {
  font-size: 3rem;
  margin-bottom: 0.5rem;
}

.subtext {
  font-size: 1.5rem;
}

.vdobutton {
  margin-top: 2rem;
  animation: fadeInUp 1s ease-in-out;
}

.robot {
  width: 80%; /* Adjust the width as needed */
  max-width: 400px; /* Set a maximum width for responsiveness */
  margin-top: 2rem;
  animation: fadeInUp 1s ease-in-out;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

 
import React from "react";
import { Link } from "react-router-dom";
import Lottie from "lottie-react";
import Robot from "../Robot1.json";
import "./HeroStyles.css";
import videobutton from "../videobutton.json";

const Hero = () => {
  return (
    <div className="hero">
      <div className="text-container">
        <h1 className="hero-text">Robotics And Automation Center</h1>
        <p className="subtext">A team of tech loving minds</p>
      </div>
      <div className="vdobutton">
        <Lottie animationData={videobutton}/>
      </div>
      <div className="robot">
        <Lottie animationData={Robot} />
      </div>
    </div>
  );
};

export default Hero;

 
/* HeroStyles.css */

/* Adjust the hero's height to 100vh to fill the viewport */
.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh; /* Set to 100vh */
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
  color: #fff; /* Text color for better contrast */
}

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: center; /* Center lotties horizontally */
  align-items: flex-start; /* Align lotties at the top */
  flex-wrap: wrap; /* Allow lotties to wrap on smaller screens */
  padding: 20px; /* Adjust padding */
  position: relative; /* Add relative positioning */
  z-index: 1; /* Place lotties above other elements */
  margin-top: -150px; /* Adjust margin to bring the Lottie container more above */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px; /* Add spacing between lotties */
}

.vdobutton {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
  margin-left: auto; /* Move the Robot towards the right */
  margin-top: -250px; /* Adjust margin to bring the Robot further above */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton,
  .robot {
    width: 150px;
    height: 150px;
  }

  .lottie-container {
    margin-top: -100px; /* Adjust margin for smaller screens */
  }

  .hero {
    align-items: center; /* Center items vertically for mobile layout */
    min-height: 100vh; /* Set min-height to 100vh for mobile */
    background-size: cover; /* Keep background-size */
  }

  .lottie-container {
    margin-top: -50px; /* Adjust margin for smaller screens */
  }
}

 
@import url('https://fonts.googleapis.com/css2?family=Alegreya+Sans:ital,wght@0,100;0,300;0,400;0,500;0,700;0,800;0,900;1,100;1,300;1,400;1,500;1,700;1,800;1,900&family=Bitter&family=Josefin+Sans:wght@100&family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Open+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,300;1,400;1,500;1,600;1,700;1,800&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&family=Source+Code+Pro&family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500&display=swap');

.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: white;
  color: black;
  padding: 20px;
  font-family: Ubuntu;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
  position: sticky;
  top: 0;
  z-index: 100;
}

.logo-container {
  flex: 1;
}

.logo {
  max-width: 100px;
}

.nav-links {
  display: flex;
  list-style: none;
  margin: 0;
  padding: 0;
}

.nav-link {
  margin-right: 20px;
}

.nav-link a {
  color: black;
  text-decoration: none;
  transition: color 0.3s;
  padding: 8px 15px;
  background-color: #f7d794;
  border-radius: 20px;
}

.nav-link a:hover {
  color: white;
  background-color: #333;
}

.hamburger-icon {
  display: none;
  font-size: 24px;
  cursor: pointer;
}

@media (max-width: 768px) {
  .hamburger-icon {
    display: block;
  }

  .nav-links {
    display: none;
    flex-direction: column;
    position: absolute;
    background-color: white;
    width: 100%;
    top: 80px;
    left: 0;
    text-align: center;
    transition: transform 0.3s;
    transform: translateY(-100%);
  }

  .hamburger-icon.active {
    display: block;
  }

  .nav-links.active {
    display: flex;
    transform: translateY(0);
  }

  .nav-link {
    margin: 15px 0;
  }

  .overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.7);
    display: none;
    z-index: 99;
  }

  .overlay.active {
    display: block;
  }
}

 
import React, { useState } from 'react';
import { NavLink } from 'react-router-dom';
import { FaBars, FaTimes } from 'react-icons/fa';
import './NavbarStyles.css';
import logo from '../assets/RAC-logo.png';

const Navbar = () => {
  const [showLinks, setShowLinks] = useState(false);

  const toggleLinks = () => {
    setShowLinks(!showLinks);
  };

  return (
    <nav className="navbar">
      <div className="logo-container">
        <img src={logo} alt="Logo" className="logo" />
      </div>
      <div className={`hamburger-icon ${showLinks ? 'active' : ''}`} onClick={toggleLinks}>
        {showLinks ? <FaTimes /> : <FaBars />}
      </div>
      <ul className={`nav-links ${showLinks ? 'active' : ''}`}>
        <li className="nav-link">
          <NavLink to="/" exact activeClassName="active">
            Home
          </NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/about" activeClassName="active">
            About
          </NavLink>
        </li>
        {/* Add more navigation links as needed */}
      </ul>
      <div className={`overlay ${showLinks ? 'active' : ''}`} onClick={toggleLinks}></div>
    </nav>
  );
};

export default Navbar;

 
import React, { useState } from 'react';
import { NavLink } from 'react-router-dom';
import { FaBars, FaTimes } from 'react-icons/fa';
import './NavbarStyles.css';
import logo from '../assets/RAC-logo.png';

const Navbar = () => {
  const [showLinks, setShowLinks] = useState(false);

  const toggleLinks = () => {
    setShowLinks(!showLinks);
  };

  return (
    <nav className="navbar">
      <div className="logo-container">
        <img src={logo} alt="Logo" className="logo" />
      </div>
      <div className={`hamburger-icon ${showLinks ? 'active' : ''}`} onClick={toggleLinks}>
        {showLinks ? <FaTimes /> : <FaBars />}
      </div>
      <ul className={`nav-links ${showLinks ? 'active' : ''}`}>
        <li className="nav-link">
          <NavLink to="/" exact activeClassName="active">
            Home
          </NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/about" activeClassName="active">
            About
          </NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/ourteam" activeClassName="active">
            Our Team
          </NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/projects" activeClassName="active">
            Projects
          </NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/alumni" activeClassName="active">
            Alumni
          </NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/workshops" activeClassName="active">
            Workshops
          </NavLink>
        </li>
      </ul>
    </nav>
  );
};

export default Navbar;

 
/* Import fonts here */

.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: white;
  color: black;
  padding: 20px;
  font-family: Ubuntu;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
  position: sticky;
  top: 0; /* Stick to the top of the viewport */
  z-index: 100; /* Ensure it's above other elements */
}

.logo-container {
  flex: 1;
}

.logo {
  max-width: 100px;
}

.nav-links {
  display: flex;
  list-style: none;
  margin: 0;
  padding: 0;
}

.nav-link {
  margin-right: 20px;
}

.nav-link a {
  color: black;
  text-decoration: none;
  transition: color 0.3s;
  padding: 8px 15px;
  background-color: #f7d794;
  border-radius: 20px;
}

.nav-link a:hover {
  color: white;
  background-color: #333;
}

.hamburger-icon {
  display: none;
  font-size: 24px;
  cursor: pointer;
}

@media (max-width: 768px) {
  .hamburger-icon {
    display: block;
  }

  .nav-links {
    display: none;
    flex-direction: column;
    position: absolute;
    background-color: white;
    width: 100%;
    top: 80px;
    left: 0;
    text-align: center;
    transition: transform 0.3s;
    transform: translateY(-100%); /* Changed this to initial state */
  }

  .hamburger-icon.active {
    display: block;
  }

  .nav-links.active {
    display: flex;
    transform: translateY(0);
  }

  .nav-link {
    margin: 15px 0;
  }
}

 
@import url('https://fonts.googleapis.com/css2?family=Alegreya+Sans:ital,wght@0,100;0,300;0,400;0,500;0,700;0,800;0,900;1,100;1,300;1,400;1,500;1,700;1,800;1,900&family=Bitter&family=Josefin+Sans:wght@100&family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Open+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,300;1,400;1,500;1,600;1,700;1,800&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&family=Source+Code+Pro&family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500&display=swap');

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: white;
    color: black;
    padding: 20px;
    font-family: Ubuntu;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1); /* Shadow effect */
  }
  
  .logo-container {
    flex: 1;
  }
  
  .logo {
    max-width: 100px;
  }
  
  .nav-links {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    margin-right: 20px;
  }
  
  .nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s;
    padding: 8px 15px;
    background-color: #f7d794;
    border-radius: 20px;
  }
  
  .nav-link a:hover {
    color: white;
    background-color: #333;
  }
  
  .hamburger-icon {
    display: none;
    font-size: 24px;
    cursor: pointer;
  }
  
  @media (max-width: 768px) {
    .hamburger-icon {
      display: block;
    }
  
    .nav-links {
      display: none;
      flex-direction: column;
      position: absolute;
      background-color: white;
      width: 100%;
      top: 80px;
      left: 0;
      text-align: center;
      transition: transform 0.3s;
      transform: translateY(${props => (props.showLinks ? '0' : '-100%')});
    }
  
    .hamburger-icon.active {
      display: block;
    }
  
    .nav-links.active {
      display: flex;
      transform: translateY(0);
    }
  
    .nav-link {
      margin: 15px 0;
    }
  }

 
/* HeroStyles.css */

/* Adjust the hero's height to 100vh to fill the viewport */
.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh; /* Set to 100vh */
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
  color: #fff; /* Text color for better contrast */
}

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: center; /* Center lotties horizontally */
  align-items: flex-start; /* Align lotties at the top */
  flex-wrap: wrap; /* Allow lotties to wrap on smaller screens */
  padding: 20px; /* Adjust padding */
  position: relative; /* Add relative positioning */
  z-index: 1; /* Place lotties above other elements */
  margin-top: -150px; /* Adjust margin to bring the Lottie container more above */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px; /* Add spacing between lotties */
}

.vdobutton {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
  margin-left: auto; /* Move the Robot towards the right */
  margin-top: -250px; /* Adjust margin to bring the Robot further above */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton,
  .robot {
    width: 150px;
    height: 150px;
  }

  .lottie-container {
    margin-top: -100px; /* Adjust margin for smaller screens */
  }

  .hero {
    align-items: flex-start; /* Align items to the top for mobile layout */
    min-height: auto; /* Reset min-height for mobile */
    background-size: auto auto; /* Adjust background size for mobile */
  }

  .lottie-container {
    margin-top: -50px; /* Adjust margin for smaller screens */
  }
}

 
/* HeroStyles.css */

/* Adjust the hero's height to 100vh to fill the viewport */
.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh; /* Set to 100vh */
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
  color: #fff; /* Text color for better contrast */
}

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: center; /* Center lotties horizontally */
  align-items: flex-start; /* Align lotties at the top */
  flex-wrap: wrap; /* Allow lotties to wrap on smaller screens */
  padding: 20px; /* Adjust padding */
  position: relative; /* Add relative positioning */
  z-index: 1; /* Place lotties above other elements */
  margin-top: -200px; /* Adjust margin to bring the Lottie container more above */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px; /* Add spacing between lotties */
}

.vdobutton {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
  margin-left: auto; /* Move the Robot towards the right */
  margin-top: -250px; /* Adjust margin to bring the Robot further above */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton,
  .robot {
    width: 150px;
    height: 150px;
  }

  .lottie-container {
    margin-top: -150px; /* Adjust margin for smaller screens */
  }
}

 
/* HeroStyles.css */

/* Adjust the hero's height to 100vh to fill the viewport */
.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh; /* Set to 100vh */
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
  color: #fff; /* Text color for better contrast */
}

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: center; /* Center lotties horizontally */
  align-items: flex-start; /* Align lotties at the top */
  flex-wrap: wrap; /* Allow lotties to wrap on smaller screens */
  padding: 20px; /* Adjust padding */
  position: relative; /* Add relative positioning */
  z-index: 1; /* Place lotties above other elements */
  margin-top: -200px; /* Adjust margin to bring the Lottie container more above */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px; /* Add spacing between lotties */
}

.vdobutton {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
  margin-left: auto; /* Move the Robot towards the right */
  margin-top: -250px; /* Adjust margin to bring the Robot further above */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton,
  .robot {
    width: 150px;
    height: 150px;
  }

  .lottie-container {
    margin-top: -150px; /* Adjust margin for smaller screens */
  }
}

 
/* HeroStyles.css */

/* Adjust the hero's height to 100vh to fill the viewport */
.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  min-height: 100vh; /* Set minimum height to 100vh to avoid scrolling */
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
  color: #fff; /* Text color for better contrast */
}

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: center; /* Center lotties horizontally */
  align-items: flex-start; /* Align lotties at the top */
  flex-wrap: wrap; /* Allow lotties to wrap on smaller screens */
  padding: 20px; /* Adjust padding */
  position: relative; /* Add relative positioning */
  z-index: 1; /* Place lotties above other elements */
  margin-top: -200px; /* Adjust margin to bring the Lottie container more above */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px; /* Add spacing between lotties */
}

.vdobutton {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
  margin-left: auto; /* Move the Robot towards the right */
  margin-top: -250px; /* Adjust margin to bring the Robot further above */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton,
  .robot {
    width: 150px;
    height: 150px;
  }

  .lottie-container {
    margin-top: -150px; /* Adjust margin for smaller screens */
  }

  .hero {
    align-items: flex-start; /* Align items to the top for mobile layout */
    padding-top: 50px; /* Add padding to top for spacing */
    min-height: auto; /* Reset min-height for mobile */
  }

  .lottie-container {
    margin-top: -100px; /* Adjust margin for smaller screens */
  }
}

 
/* HeroStyles.css */

/* Adjust the hero's height to 100vh to fill the viewport */
.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh; /* Set to 100vh */
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
  color: #fff; /* Text color for better contrast */
}

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: center; /* Center lotties horizontally */
  align-items: flex-start; /* Align lotties at the top */
  flex-wrap: wrap; /* Allow lotties to wrap on smaller screens */
  padding: 20px; /* Adjust padding */
  position: relative; /* Add relative positioning */
  z-index: 1; /* Place lotties above other elements */
  margin-top: -200px; /* Adjust margin to bring the Lottie container more above */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px; /* Add spacing between lotties */
}

.vdobutton {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
  margin-left: auto; /* Move the Robot towards the right */
  margin-top: -250px; /* Adjust margin to bring the Robot further above */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton,
  .robot {
    width: 150px;
    height: 150px;
  }

  .lottie-container {
    margin-top: -150px; /* Adjust margin for smaller screens */
  }
}

 
/* HeroStyles.css */

/* Adjust the hero's height to 100vh to fill the viewport */
.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh; /* Set to 100vh */
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
  color: #fff; /* Text color for better contrast */
}

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: center; /* Center lotties horizontally */
  align-items: flex-start; /* Align lotties at the top */
  flex-wrap: wrap; /* Allow lotties to wrap on smaller screens */
  padding: 20px; /* Adjust padding */
  position: relative; /* Add relative positioning */
  z-index: 1; /* Place lotties above other elements */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px; /* Add spacing between lotties */
}

.vdobutton {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
  margin-left: auto; /* Move the Robot towards the right */
  margin-top: -200px; /* Adjust margin to bring the Robot above */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton,
  .robot {
    width: 150px;
    height: 150px;
  }
}

 
/* HeroStyles.css */

/* Adjust the hero's height to 100vh to fill the viewport */
.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh; /* Set to 100vh */
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
  color: #fff; /* Text color for better contrast */
}

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: center; /* Center lotties horizontally */
  align-items: flex-start; /* Align lotties at the top */
  flex-wrap: wrap; /* Allow lotties to wrap on smaller screens */
  padding: 20px; /* Adjust padding */
  position: relative; /* Add relative positioning */
  z-index: 1; /* Place lotties above other elements */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px; /* Add spacing between lotties */
}

.vdobutton {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
  margin-left: auto; /* Move the Robot towards the right */
  margin-top: -200px; /* Adjust margin to bring the Robot above */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton,
  .robot {
    width: 150px;
    height: 150px;
  }
}

 
/* HeroStyles.css */

/* HeroStyles.css */

/* Adjust the hero's height to 100vh to fill the viewport */
.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh; /* Set to 100vh */
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
}

/* ... (Rest of your styles) ... */

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
  color: #fff;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
  color: #fff;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: center; /* Center lotties horizontally */
  align-items: flex-start; /* Align lotties at the top */
  flex-wrap: wrap; /* Allow lotties to wrap on smaller screens */
  padding: 20px; /* Adjust padding */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px; /* Add spacing between lotties */
}

.vdobutton {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
  margin-left: auto; /* Move the Robot towards the right */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton,
  .robot {
    width: 150px;
    height: 150px;
  }
}

 
.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
}

 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
}

/* ... (Rest of your styles) ... */

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
  color: #fff;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
  color: #fff;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: center; /* Center lotties horizontally */
  align-items: center;
  flex-wrap: wrap; /* Allow lotties to wrap on smaller screens */
  padding: 20px; /* Adjust padding */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px; /* Add spacing between lotties */
}

.vdobutton {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
  margin-left: auto; /* Move the Robot towards the right */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton,
  .robot {
    width: 150px;
    height: 150px;
  }
}

 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
}

/* ... (Rest of your styles) ... */

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
  color: #fff;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
  color: #fff;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: center; /* Center lotties horizontally */
  align-items: center;
  flex-wrap: wrap; /* Allow lotties to wrap on smaller screens */
  padding: 20px; /* Adjust padding */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 10px; /* Add spacing between lotties */
}

.vdobutton {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton,
  .robot {
    width: 150px;
    height: 150px;
  }
}

 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
}

/* ... (Rest of your styles) ... */

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
  color: #fff;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
  color: #fff;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: space-around; /* Distribute lotties across the space */
  align-items: center;
  padding: 20px; /* Adjust padding */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
}

.vdobutton {
  width: 200px; /* Adjust width */
  height: 200px; /* Adjust height */
  margin-right: 50px; /* Add margin between lotties */
}

.robot {
  width: 400px; /* Adjust width */
  height: 400px; /* Adjust height */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton {
    width: 150px;
    height: 150px;
    margin-right: 20px;
  }

  .robot {
    width: 300px;
    height: 300px;
  }
}



 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
}

/* ... (Rest of your styles) ... */

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
  color: #fff;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
  color: #fff;
}

/* Centering the Lottie animations */
.lottie-container {
  display: flex;
  justify-content: space-around; /* Distribute lotties across the space */
  align-items: center;
  padding: 20px; /* Adjust padding */
}

.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
}

.vdobutton {
  width: 150px; /* Adjust width */
  height: 150px; /* Adjust height */
  margin-right: 20px; /* Add margin between lotties */
}

.robot {
  width: 250px; /* Adjust width */
  height: 250px; /* Adjust height */
}

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton {
    width: 100px;
    height: 100px;
  }

  .robot {
    width: 150px;
    height: 150px;
  }
}

 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
}

/* ... (Rest of your styles) ... */

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
  color: #fff;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
  color: #fff;
}

/* Centering the Lottie animations */
.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px; /* Adjust padding */
}

.vdobutton {
  width: 100px; /* Adjust width */
  height: 100px; /* Adjust height */
  margin-bottom: 20px; /* Add margin at bottom */
}

.robot {
  width: 150px; /* Adjust width */
  height: 150px; /* Adjust height */
}

/* ... (Rest of your styles) ... */

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton {
    width: 80px;
    height: 80px;
    margin-bottom: 10px;
  }

  .robot {
    width: 120px;
    height: 120px;
  }
}

 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  flex-direction: column; /* Stack elements vertically */
  justify-content: space-between; /* Distribute elements across the space */
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
}

/* ... (Rest of your styles) ... */

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: -1;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2rem; /* Adjust font size */
  margin-bottom: 10px;
  color: #fff;
}

/* Subtext */
.subtext {
  font-size: 1rem; /* Adjust font size */
  opacity: 0.8;
  color: #fff;
}

/* Centering the Lottie animations */
.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 10px; /* Adjust padding */
}

.vdobutton {
  width: 80px; /* Adjust width */
  height: 80px; /* Adjust height */
  margin-bottom: 20px; /* Add margin at bottom */
}

.robot {
  width: 120px; /* Adjust width */
  height: 120px; /* Adjust height */
}

/* ... (Rest of your styles) ... */

/* Responsiveness */
@media (max-width: 768px) {
  .hero-text {
    font-size: 1.5rem;
  }

  .subtext {
    font-size: 1rem;
  }

  .vdobutton {
    width: 60px;
    height: 60px;
    margin-bottom: 10px;
  }

  .robot {
    width: 90px;
    height: 90px;
  }
}

 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
}

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(255, 255, 255, 0.2); /* Adjust the alpha value */
  z-index: -1;
}

/* ... (Rest of your styles) ... */

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative; /* Ensure the text is above the mask layer */
  z-index: 1; /* Bring the text above the mask layer */
}

/* Hero text */
.hero-text {
  font-size: 2.5rem;
  margin-bottom: 10px;
  color: #fff; /* Set text color */
}

/* Subtext */
.subtext {
  font-size: 1.2rem;
  opacity: 0.8;
  color: #fff; /* Set text color */
}

/* ... (Rest of your styles) ... */


 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
}

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.4); /* Adjust the alpha value and color */
  z-index: -1;
}

/* ... (Rest of your styles) ... */

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative;
  z-index: 1;
}

/* Hero text */
.hero-text {
  font-size: 2.5rem;
  margin-bottom: 10px;
  color: #fff;
}

/* Subtext */
.subtext {
  font-size: 1.2rem;
  opacity: 0.8;
  color: #fff;
}

/* ... (Rest of your styles) ... */

 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-color: rgba(0, 0, 0, 0.6);
  overflow: hidden;
}

/* White mask layer */
.hero::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(255, 255, 255, 0.2); /* Adjust the alpha value */
  z-index: -1;
}

/* ... (Rest of your styles) ... */

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
  position: relative; /* Ensure the text is above the mask layer */
  z-index: 1; /* Bring the text above the mask layer */
}

/* Hero text */
.hero-text {
  font-size: 2.5rem;
  margin-bottom: 10px;
  color: #fff; /* Set text color */
}

/* Subtext */
.subtext {
  font-size: 1.2rem;
  opacity: 0.8;
  color: #fff; /* Set text color */
}

/* ... (Rest of your styles) ... */


 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-blend-mode: overlay;
  background-color: rgba(0, 0, 0, 0.6);
  color: #fff;
  overflow: hidden;
}

/* Curved shape at the bottom */
.hero::before {
  content: "";
  position: absolute;
  bottom: -50px;
  left: 0;
  right: 0;
  height: 100px;
  background: linear-gradient(to bottom, transparent, rgba(0, 0, 0, 0.6));
  z-index: -1;
  border-radius: 50% / 100px 100px 0 0;
}

/* Centering the Lottie animations */
.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  padding: 20px;
}

.vdobutton {
  width: 200px;
  height: 200px;
  margin-right: 20px;
}

.robot {
  width: 300px;
  height: 300px;
}

/* Additional styling for Lottie animations */
.lottie-container {
  overflow: hidden;
}

/* Text container */
.text-container {
  text-align: center;
  margin: 0 auto;
  padding: 20px;
}

/* Hero text */
.hero-text {
  font-size: 2.5rem;
  margin-bottom: 10px;
}

/* Subtext */
.subtext {
  font-size: 1.2rem;
  opacity: 0.8;
}

/* Responsiveness */
@media (max-width: 768px) {
  .vdobutton {
    margin-right: 0;
    margin-bottom: 20px;
  }
}

 
/* HeroStyles.css */

.hero {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  
}

.vdobutton {
  width: 200px;
  height: 200px;
  margin-right: 20px;
}

.robot {
  width: 300px;
  height: 300px;
}

/* Additional styling for Lottie animations */
.lottie-container {
  overflow: hidden;
}

 
import React from "react";
import { Link } from "react-router-dom";
import Lottie from "lottie-react";
import Robot from "../Robot1.json";
import "./HeroStyles.css";
import videobutton from "../videobutton.json";


const Hero = () => {
  return (
    <div className="hero">
      <div className="vdobutton">
        <Lottie animationData={videobutton}/>
      </div>
      <div className="robot">
        <Lottie animationData={Robot} />
      </div>
    </div>
  );
};

export default Hero;

 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-position: center;
  background-blend-mode: overlay;
  background-color: rgba(0, 0, 0, 0.6);
  color: #fff;
  overflow: hidden; /* Hide overflowing curved shape */
}

/* Curved shape at the bottom */
.hero::before {
  content: "";
  position: absolute;
  bottom: -50px; /* Adjust the curvature */
  left: 0;
  right: 0;
  height: 100px; /* Adjust the curvature height */
  background: linear-gradient(to bottom, transparent, rgba(0, 0, 0, 0.6));
  z-index: -1;
  border-radius: 50% / 100px 100px 0 0; /* Create the curve */
}

/* Centering the Lottie animations */
.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  padding: 20px;
}

.vdobutton {
  width: 200px;
  height: 200px;
  margin-right: 20px;
}

.robot {
  width: 300px;
  height: 300px;
}

/* Additional styling for Lottie animations */
.lottie-container {
  overflow: hidden;
}

/* Responsiveness */
@media (max-width: 768px) {
  .vdobutton {
    margin-right: 0;
    margin-bottom: 20px;
  }
}

 
background-image: url("../assets/bgf.jpg");
 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bgf.jpg");
  background-size: cover;
  background-position: center;
  background-blend-mode: overlay; /* This will apply a mask effect */
  background-color: rgba(0, 0, 0, 0.6); /* Adjust the alpha value for the mask */
  color: #fff; /* Text color for better contrast */
}

/* Centering the Lottie animations */
.vdobutton,
.robot {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  padding: 20px;
}

.vdobutton {
  width: 200px;
  height: 200px;
  margin-right: 20px;
}

.robot {
  width: 300px;
  height: 300px;
}

/* Additional styling for Lottie animations */
.lottie-container {
  overflow: hidden;
}

 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-image: url("../assets/pcb.jpg");
  background-size: cover;
  background-position: center;
  background-blend-mode: overlay; /* This will apply a mask effect */
  background-color: rgba(0, 0, 0, 0.6); /* Adjust the alpha value for the mask */
  color: #fff; /* Text color for better contrast */
}

/* Centering the Lottie animations */
.vdobutton,
.robot 
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  padding: 20px;
}

.vdobutton {
  width: 200px;
  height: 200px;
  margin-right: 20px;
}

.robot {
  width: 300px;
  height: 300px;
}

/* Additional styling for Lottie animations */
.lottie-container {
  overflow: hidden;
}

 
/* HeroStyles.css */

.hero {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-image: url("../assets/bg2.avif");
  background-size: cover;
  background-position: center;
  background-blend-mode: overlay; /* This will apply a mask effect */
  background-color: rgba(0, 0, 0, 0.6); /* Adjust the alpha value for the mask */
}

.vdobutton {
  width: 200px;
  height: 200px;
  margin-right: 20px;
}

.robot {
  width: 300px;
  height: 300px;
}

/* Additional styling for Lottie animations */
.lottie-container {
  overflow: hidden;
}

 
/* HeroStyles.css */

.hero {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  
}

.vdobutton {
  width: 200px;
  height: 200px;
  margin-right: 20px;
}

.robot {
  width: 300px;
  height: 300px;
}

/* Additional styling for Lottie animations */
.lottie-container {
  overflow: hidden;
}

 
background-color: #f0f0f0;
 
/* HeroStyles.css */
.hero {
    display: flex;
    justify-content: center; /* Center horizontally */
    align-items: center;
    height: 100vh; /* Ensure the content takes up the full viewport height */
  }
  
  .content {
    max-width: 600px;
    text-align: center; /* Center text */
  }
  
  .content p {
    font-size: 18px;
    color: #777;
  }
  
  .content h1 {
    font-size: 48px;
    margin-top: 20px;
    color: #333;
  }
  
  .content a.btn,
  .content a.btn-light {
    display: inline-block;
    margin-top: 20px;
    margin-right: 10px; /* Add some spacing between buttons */
    padding: 12px 24px;
    font-size: 16px;
    text-align: center;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: 5px;
    transition: background-color 0.3s ease, color 0.3s ease;
  }
  
  .content a.btn {
    background-color: #fdcb6e;
    color: black;
  }
  
  .content a.btn-light {
    background-color: transparent;
    color: black;
    border: 2px solid #ffeaa7;
  }
  
  .content a.btn:hover,
  .content a.btn-light:hover {
    background-color:#ffeaa7;
    color: #black;
  }
  
  .robot {
    width: 30%; /* Adjust size as needed */
  }
  
  .text-container {
    display: flex;
    flex-direction: column;
    align-items: center; /* Center text vertically */
  }
  
  .text-container p {
    font-size: 24px;
    font-weight: bold;
    color: #333;
    margin-bottom: 10px;
  }
  
  .text-container h1 {
    font-size: 48px;
    margin-bottom: 20px;
    color: #3498db;
  }
  
  .text-container .btn,
  .text-container .btn-light {
    font-size: 18px;
    padding: 10px 20px;
    border-radius: 5px;
  }
  
  .text-container .btn {
    background-color: #3498db;
    color: #fff;
    margin-right: 10px;
  }
  
  .text-container .btn-light {
    background-color: #fff;
    color: #3498db;
    border: 2px solid #3498db;
  }
  
  .text-container .btn:hover,
  .text-container .btn-light:hover {
    background-color: #2980b9;
    color: #fff;
  }
  

  
 
{/* <div className="content">
        <p>A team Of Tech Loving Minds</p>
        <h1>RAC</h1>
        <div>
            <Link to="/project" className="btn">Projects</Link>
            <Link to="/about" className="btn-light">About</Link>
  </div> 
    </div> */}
 
.vdobutton{
    width:30%;
    height:auto;
  }
 
/* HeroStyles.css */
.hero {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 50px 20px;
  }
  
  .content {
    flex: 1;
    max-width: 600px;
    margin-right: 40px;
  }
  
  .content p {
    font-size: 18px;
    color: #777;
  }
  
  .content h1 {
    font-size: 48px;
    margin-top: 20px;
    color: #333;
  }
  
  .content a.btn,
  .content a.btn-light {
    display: inline-block;
    margin-top: 20px;
    padding: 12px 24px;
    font-size: 16px;
    text-align: center;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: 5px;
    transition: background-color 0.3s ease, color 0.3s ease;
  }
  
  .content a.btn {
    background-color: #3498db;
    color: #fff;
  }
  
  .content a.btn-light {
    background-color: transparent;
    color: #3498db;
    border: 2px solid #3498db;
  }
  
  .content a.btn:hover,
  .content a.btn-light:hover {
    background-color: #2980b9;
    color: #fff;
  }
  
  .robot {
    width: 30%; /* Smaller size */
    height: auto;
  }
  
  /* New styles for text container */
  .text-container {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
  }
  
  .text-container p {
    font-size: 24px;
    font-weight: bold;
    color: #333;
    margin-bottom: 10px;
  }
  
  .text-container h1 {
    font-size: 48px;
    margin-bottom: 20px;
    color: #3498db;
  }
  
  .text-container .btn,
  .text-container .btn-light {
    font-size: 18px;
    padding: 10px 20px;
    border-radius: 5px;
  }
  
  .text-container .btn {
    background-color: #3498db;
    color: #fff;
    margin-right: 10px;
  }
  
  .text-container .btn-light {
    background-color: #fff;
    color: #3498db;
    border: 2px solid #3498db;
  }
  
  .text-container .btn:hover,
  .text-container .btn-light:hover {
    background-color: #2980b9;
    color: #fff;
  }
  
 
/* HeroStyles.css */
.hero {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 50px 20px;
  }
  
  .content {
    flex: 1;
    max-width: 600px;
    margin-right: 40px;
  }
  
  .content p {
    font-size: 18px;
    color: #777;
  }
  
  .content h1 {
    font-size: 48px;
    margin-top: 20px;
    color: #333;
  }
  
  .content a.btn,
  .content a.btn-light {
    display: inline-block;
    margin-top: 20px;
    padding: 12px 24px;
    font-size: 16px;
    text-align: center;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: 5px;
    transition: background-color 0.3s ease, color 0.3s ease;
  }
  
  .content a.btn {
    background-color: #3498db;
    color: #fff;
  }
  
  .content a.btn-light {
    background-color: transparent;
    color: #3498db;
    border: 2px solid #3498db;
  }
  
  .content a.btn:hover,
  .content a.btn-light:hover {
    background-color: #2980b9;
    color: #fff;
  }
  
  .robot {
    width: 50%;
    height: 100%;
  }
  
 

.robot {
    width: 300px;
    height: 300px;
    margin: 50px auto 0 auto; /* Added margin-top and removed marginTop */
    padding: 20px;
    
     
  }
  
 
</div>
 

import React from "react";
import {Link} from "react-router-dom";
import Lottie from "lottie-react"
import Robot from "../Robot.json";
import './HeroStyles.css'




const Hero = () => {
  return(
<div className="robot">
    <Lottie animationData={Robot}/>
</div>

  );
};

export default Hero;

 
width: 300px;
    height: 300px;
    margin: 0 auto;
    margin-top: 50px;
    padding: 20px; /* Add padding as needed */
 
/* Add a border for visualization */
    box-sizing: border-box; /* Include padding in width and height */
 
.robot{
    width: 300px;
    height: 300px;
    margin: 0 auto;
    marginTop: 50px;
}
 
 <Navbar/>
   <Hero/>
 
import Hero from "./components/Hero";

 
return( <div className="hero">
    <div className="mask">
        <img className="into-img" src={IntroImg} alt="IntroImg" />
    </div>
    <div className="content">
        <p>Hello World, I am Jyotsna Jha</p>
        <h1>Web Developer.</h1>
        <div>
            <Link to="/project" className="btn">Projects</Link>
            <Link to="/contact" className="btn-light">Contact</Link>
        </div>
    </div>
  </div>
 
@import url('https://fonts.googleapis.com/css2?family=Alegreya+Sans:ital,wght@0,100;0,300;0,400;0,500;0,700;0,800;0,900;1,100;1,300;1,400;1,500;1,700;1,800;1,900&family=Bitter&family=Josefin+Sans:wght@100&family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Open+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,300;1,400;1,500;1,600;1,700;1,800&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&family=Source+Code+Pro&family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500&display=swap');

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: white; /* Dark background color */
    color: black; /* Text color */
    padding: 20px;
    font-family: Ubuntu;
  }
  
  .logo-container {
    flex: 1;
  }
  
  .logo {
    max-width: 100px;
  }
  
  .nav-links {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    margin-right: 20px;
  }
  
  .nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s;
    padding: 8px 15px;
    background-color: #f7d794; /* Light yellow background color */
    border-radius: 20px; /* Rounded box */
  }
  
  .nav-link a:hover {
    color: #f5cd79; /* Light brown hover color */
    background-color: #333; /* Darker background color on hover */
  }
  
  .hamburger-icon {
    display: none;
    font-size: 24px;
    cursor: pointer;
  }
  
  @media (max-width: 768px) {
    .hamburger-icon {
      display: block;
    }
  
    .nav-links {
      display: none;
      flex-direction: column;
      position: absolute;
      background-color: white;
      width: 100%;
      top: 80px;
      left: 0;
      text-align: center;
      transition: transform 0.3s;
      transform: translateY(${props => (props.showLinks ? '0' : '-100%')});
    }
  
    .hamburger-icon.active {
      display: block;
    }
  
    .nav-links.active {
      display: flex;
      transform: translateY(0);
    }
  
    .nav-link {
      margin: 15px 0;
    }
  }
  
 
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #1a1a1a; /* Dark background color */
    color: #fff; /* Text color */
    padding: 20px;
  }
  
  .logo-container {
    flex: 1;
  }
  
  .logo {
    max-width: 100px;
  }
  
  .nav-links {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    margin-right: 20px;
  }
  
  .nav-link a {
    color: #fff;
    text-decoration: none;
    transition: color 0.3s;
    padding: 8px 15px;
    background-color: #f5cd79; /* Light brown background color */
    border-radius: 20px; /* Rounded box */
  }
  
  .nav-link a:hover {
    color: #ff7f50; /* Orange hover color */
    background-color: #f7d794; /* Lighter brown background color on hover */
  }
  
  .hamburger-icon {
    display: none;
    font-size: 24px;
    cursor: pointer;
  }
  
  @media (max-width: 768px) {
    .hamburger-icon {
      display: block;
    }
  
    .nav-links {
      display: none;
      flex-direction: column;
      position: absolute;
      background-color: #1a1a1a;
      width: 100%;
      top: 80px;
      left: 0;
      text-align: center;
      transition: transform 0.3s;
      transform: translateY(${props => (props.showLinks ? '0' : '-100%')});
    }
  
    .hamburger-icon.active {
      display: block;
    }
  
    .nav-links.active {
      display: flex;
      transform: translateY(0);
    }
  
    .nav-link {
      margin: 15px 0;
    }
  }
  
 
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #f0f0f0;
    color: #333;
    padding: 20px;
  }
  
  .logo-container {
    flex: 1;
  }
  
  .logo {
    max-width: 100px;
  }
  
  .nav-links {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    margin-right: 20px;
  }
  
  .nav-link a {
    color: #333;
    text-decoration: none;
    transition: color 0.3s;
  }
  
  .nav-link a:hover {
    color: #ff7f50;
  }
  
  .hamburger-icon {
    display: none;
    font-size: 24px;
    cursor: pointer;
  }
  
  @media (max-width: 768px) {
    .hamburger-icon {
      display: block;
    }
  
    .nav-links {
      display: none;
      flex-direction: column;
      position: absolute;
      background-color: #f0f0f0;
      width: 100%;
      top: 80px;
      left: 0;
      text-align: center;
      transition: transform 0.3s;
      transform: translateY(${props => (props.showLinks ? '0' : '-100%')});
    }
  
    .hamburger-icon.active {
      display: block;
    }
  
    .nav-links.active {
      display: flex; /* Show the nav-links when active */
      transform: translateY(0);
    }
  
    .nav-link {
      margin: 15px 0;
    }
  }
  
 
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #f0f0f0;
    color: #333;
    padding: 20px;
  }
  
  .logo-container {
    flex: 1;
  }
  
  .logo {
    max-width: 100px;
  }
  
  .nav-links {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    margin-right: 20px;
  }
  
  .nav-link a {
    color: #333;
    text-decoration: none;
    transition: color 0.3s;
  }
  
  .nav-link a:hover {
    color: #ff7f50;
  }
  
  .hamburger-icon {
    display: none;
    font-size: 24px;
    cursor: pointer;
  }
  
  @media (max-width: 768px) {
    .hamburger-icon {
      display: block;
    }
  
    .nav-links {
      display: none;
      flex-direction: column;
      position: absolute;
      background-color: #f0f0f0;
      width: 100%;
      top: 80px;
      left: 0;
      text-align: center;
      transition: transform 0.3s;
      transform: translateY(${props => (props.showLinks ? '0' : '-100%')});
    }
  
    .hamburger-icon.active {
      display: block;
    }
  
    .nav-links.active {
      display: flex; /* Show the nav-links when active */
      transform: translateY(0);
    }
  
    .nav-link {
      margin: 15px 0;
    }
  }
  
 
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #f0f0f0;
    color: #333;
    padding: 20px;
  }
  
  .logo-container {
    flex: 1;
  }
  
  .logo {
    max-width: 100px;
  }
  
  .nav-links {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    margin-right: 20px;
  }
  
  .nav-link a {
    color: #333;
    text-decoration: none;
    transition: color 0.3s;
  }
  
  .nav-link a:hover {
    color: #ff7f50;
  }
  
  .hamburger-icon {
    display: none;
    font-size: 24px;
    cursor: pointer;
  }
  
  @media (max-width: 768px) {
    .hamburger-icon {
      display: block;
    }
  
    .nav-links {
      display: flex;
      flex-direction: column;
      position: absolute;
      background-color: #f0f0f0;
      width: 100%;
      top: 80px;
      left: 0;
      text-align: center;
      transition: transform 0.3s;
      transform: translateY(${props => (props.showLinks ? '0' : '-100%')});
    }
  
    .hamburger-icon.active {
      display: block;
    }
  
    .nav-links.active {
      transform: translateY(0);
    }
  
    .nav-link {
      margin: 15px 0;
    }
  }
  
 
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #f0f0f0;
    color: #333;
    padding: 20px;
  }
  
  .logo-container {
    flex: 1;
  }
  
  .logo {
    max-width: 100px;
  }
  
  .nav-links {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    margin-right: 20px;
  }
  
  .nav-link a {
    color: #333;
    text-decoration: none;
    transition: color 0.3s;
  }
  
  .nav-link a:hover {
    color: #ff7f50;
  }
  
  .hamburger-icon {
    display: none;
    font-size: 24px;
    cursor: pointer;
  }
  
  @media (max-width: 768px) {
    .nav-links {
      display: none;
      flex-direction: column;
      position: absolute;
      background-color: #f0f0f0;
      width: 100%;
      top: 80px;
      left: 0;
      text-align: center;
      transition: transform 0.3s;
      transform: translateY(-100%);
    }
  
    .hamburger-icon {
      display: block;
    }
  
    .hamburger-icon.active {
      display: block;
    }
  
    .nav-links.active {
      transform: translateY(0);
    }
  
    .nav-link {
      margin: 15px 0;
    }
  }
  


  /* Your other styles */

.hamburger-icon {
    display: none;
    font-size: 24px;
    cursor: pointer;
  }
  
  @media (max-width: 768px) {
    .hamburger-icon {
      display: block;
    }
  
    .nav-links {
      display: flex;
      flex-direction: column;
      position: absolute;
      background-color: #f0f0f0;
      width: 100%;
      top: 80px;
      left: 0;
      text-align: center;
      transition: transform 0.3s;
      transform: translateY(${props => (props.showLinks ? '0' : '-100%')});
    }
  
    /* Your other styles */
  }
  
 
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #1a1a1a;
    color: #fff;
    padding: 20px;
  }
  
  .logo-container {
    flex: 1;
  }
  
  .logo {
    max-width: 100px;
  }
  
  .nav-links {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    margin-right: 20px;
  }
  
  .nav-link a {
    color: #fff;
    text-decoration: none;
    transition: color 0.3s;
  }
  
  .nav-link a:hover {
    color: #ff7f50;
  }
  
  .hamburger-icon {
    display: none;
    font-size: 24px;
    cursor: pointer;
  }
  
  @media (max-width: 768px) {
    .nav-links {
      display: none;
      flex-direction: column;
      position: absolute;
      background-color: #1a1a1a;
      width: 100%;
      top: 80px;
      left: 0;
      text-align: center;
      transition: transform 0.3s;
      transform: translateY(-100%);
    }
  
    .hamburger-icon {
      display: block;
    }
  
    .hamburger-icon.active {
      display: block;
    }
  
    .nav-links.active {
      transform: translateY(0);
    }
  
    .nav-link {
      margin: 15px 0;
    }
  }
  
 
import React, { useState } from 'react';
import { NavLink } from 'react-router-dom';
import { FaBars, FaTimes } from 'react-icons/fa';
import './NavbarStyles.css';
import logo from '../assets/RAC-logo.png';

const Navbar = () => {
  const [showLinks, setShowLinks] = useState(false);

  const toggleLinks = () => {
    setShowLinks(!showLinks);
  };

  return (
    <nav className="navbar">
      <div className="logo-container">
        <img src={logo} alt="Logo" className="logo" />
      </div>
      <div className="hamburger-icon" onClick={toggleLinks}>
        {showLinks ? <FaTimes /> : <FaBars />}
      </div>
      <ul className={`nav-links ${showLinks ? 'active' : ''}`}>
        <li className="nav-link">
          <NavLink to="/" exact activeClassName="active">
            Home
          </NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/about" activeClassName="active">
            About
          </NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/ourteam" activeClassName="active">
            Our Team
          </NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/projects" activeClassName="active">
            Projects
          </NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/alumni" activeClassName="active">
            Alumni
          </NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/workshops" activeClassName="active">
            Workshops
          </NavLink>
        </li>
      </ul>
    </nav>
  );
};

export default Navbar;

 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    margin-right: 20px;
    display: none; /* Initially hidden on larger screens */
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7;
    border-radius: 25px;
    padding: 10px 20px;
    text-align: center;
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

/* ... Other styles ... */

/* Responsive styles */
@media (max-width: 768px) {
    .navbar {
        padding: 20px;
    }

    .nav-links {
        display: none; /* Hide navigation links by default */
        flex-direction: column;
        position: absolute;
        top: 70px; /* Adjust the top position */
        left: 0;
        width: 100%;
        background-color: white;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }

    .nav-links.active {
        display: flex; /* Show navigation links when active */
    }

    .hamburger-icon {
        display: block; /* Show the hamburger icon on small screens */
    }
}

 
/* NavbarStyles.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    margin-right: 20px;
    display: none; /* Initially hidden on larger screens */
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7;
    border-radius: 25px;
    padding: 10px 20px;
    text-align: center;
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

.nav-link a {
    color: #2d3436; /* Set link color */
    text-decoration: none; /* Remove underline */
    transition: color 0.3s ease-in-out;
}

.nav-link:hover {
    background-color: #fdcb6e; /* Hover background color */
    transform: scale(1.05); /* Slightly increase size on hover */
}

.nav-link a:hover {
    color: black; /* Hover link color */
}

/* Apply styles to the active link */
.nav-link.active {
    background-color: #e74c3c;
}

/* Responsive styles */
@media (max-width: 768px) {
    .navbar {
        padding: 20px;
    }

    .nav-links {
        display: none; /* Hide navigation links by default */
        flex-direction: column;
        position: absolute;
        top: 70px; /* Adjust the top position */
        left: 0;
        width: 100%;
        background-color: white;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }

    .nav-links.active {
        display: flex; /* Show navigation links when active */
    }

    .hamburger-icon {
        display: block; /* Show the hamburger icon on small screens */
        position: relative;
        z-index: 1001; /* Ensure it's above the nav links */
    }

    /* Hide the box enclosing nav links */
    .nav-links {
        display: none;
    }
}

 
/* NavbarStyles.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    margin-right: 20px;
    display: none; /* Initially hidden on larger screens */
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7;
    border-radius: 25px;
    padding: 10px 20px;
    text-align: center;
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

.nav-link a {
    color: #2d3436; /* Set link color */
    text-decoration: none; /* Remove underline */
    transition: color 0.3s ease-in-out;
}

.nav-link:hover {
    background-color: #fdcb6e; /* Hover background color */
    transform: scale(1.05); /* Slightly increase size on hover */
}

.nav-link a:hover {
    color: black; /* Hover link color */
}

/* Apply styles to the active link */
.nav-link.active {
    background-color: #e74c3c;
}

/* Responsive styles */
@media (max-width: 768px) {
    .navbar {
        padding: 20px;
    }

    .nav-links {
        display: none; /* Hide navigation links by default */
        flex-direction: column;
        position: absolute;
        top: 70px; /* Adjust the top position */
        left: 0;
        width: 100%;
        background-color: white;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }

    .nav-links.active {
        display: flex; /* Show navigation links when active */
    }

    .hamburger-icon {
        display: block; /* Show the hamburger icon on small screens */
        position: relative;
        z-index: 1001; /* Ensure it's above the nav links */
    }

    /* Hide the box enclosing nav links */
    .nav-links {
        display: none;
    }
}

 
/* NavbarStyles.css */

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #ffeaa7;
    padding: 10px 20px;
    position: relative;
  }
  
  .logo-container {
    flex: 1;
    display: flex;
    align-items: center;
  }
  
  .logo {
    max-width: 100px;
    height: auto;
  }
  
  .nav-links {
    list-style: none;
    display: flex;
    margin: 0;
    padding: 0;
    position: absolute;
    top: 70px; /* Adjust this value to control the distance from the top */
    right: 20px;
    background-color: #ffeaa7;
    flex-direction: column;
    text-align: right;
    opacity: 0;
    pointer-events: none;
    transition: all 0.3s ease-in-out;
  }
  
  .nav-links.active {
    opacity: 1;
    pointer-events: auto;
    top: 100px; /* Adjust this value to control the distance from the top */
  }
  
  .nav-link {
    margin: 10px 0;
  }
  
  .nav-link a {
    text-decoration: none;
    color: #333;
    font-weight: bold;
    transition: color 0.3s ease-in-out;
  }
  
  .nav-link a.active {
    color: #ff6b6b;
  }
  
  .hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    display: none;
  }
  
  @media (max-width: 768px) {
    .nav-links {
      top: 100px; /* Adjust this value for small screens */
      right: 0;
      width: 100%;
      flex-direction: column;
      align-items: center;
      background-color: #ffeaa7;
    }
  
    .nav-links.active {
      top: 70px; /* Adjust this value for small screens */
    }
  
    .hamburger-icon {
      display: block;
    }
  
    .nav-links.active {
      opacity: 1;
      pointer-events: auto;
    }
  
    .nav-links.active {
      top: 70px; /* Adjust this value for small screens */
    }
  }
  
 
/* NavbarStyles.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    margin-right: 20px;
    display: none; /* Initially hidden on larger screens */
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7;
    border-radius: 25px;
    padding: 10px 20px;
    text-align: center;
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

.nav-link a {
    color: #2d3436; /* Set link color */
    text-decoration: none; /* Remove underline */
    transition: color 0.3s ease-in-out;
}

.nav-link:hover {
    background-color: #fdcb6e; /* Hover background color */
    transform: scale(1.05); /* Slightly increase size on hover */
}

.nav-link a:hover {
    color: black; /* Hover link color */
}

/* Apply styles to the active link */
.nav-link.active {
    background-color: #e74c3c;
}

/* Responsive styles */
@media (max-width: 768px) {
    .navbar {
        padding: 20px;
    }

    .nav-links {
        display: none; /* Hide navigation links by default */
        flex-direction: column;
        position: absolute;
        top: 70px; /* Adjust the top position */
        left: 0;
        width: 100%;
        background-color: white;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }

    .nav-links.active {
        display: flex; /* Show navigation links when active */
    }

    .hamburger-icon {
        display: block; /* Show the hamburger icon on small screens */
        position: relative;
        z-index: 1001; /* Ensure it's above the nav links */
    }

    /* Hide the box enclosing nav links */
    .nav-links {
        display: none;
    }
}

 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    margin-right: 20px;
    display: none; /* Initially hidden on larger screens */
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7;
    border-radius: 25px;
    padding: 10px 20px;
    text-align: center;
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

/* ... Other styles ... */

/* Responsive styles */
@media (max-width: 768px) {
    .navbar {
        padding: 20px;
    }

    .nav-links {
        display: none; /* Hide navigation links by default */
        flex-direction: column;
        position: absolute;
        top: 70px; /* Adjust the top position */
        left: 0;
        width: 100%;
        background-color: white;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }

    .nav-links.active {
        display: flex; /* Show navigation links when active */
    }

    .hamburger-icon {
        display: block; /* Show the hamburger icon on small screens */
    }
}

 
/* NavbarStyles.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    margin-right: 20px;
    display: none; /* Initially hidden on larger screens */
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7;
    border-radius: 25px;
    padding: 10px 20px;
    text-align: center;
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

.nav-link a {
    color: #2d3436; /* Set link color */
    text-decoration: none; /* Remove underline */
    transition: color 0.3s ease-in-out;
}

.nav-link:hover {
    background-color: #fdcb6e; /* Hover background color */
    transform: scale(1.05); /* Slightly increase size on hover */
}

.nav-link a:hover {
    color: black; /* Hover link color */
}

/* Apply styles to the active link */
.nav-link.active {
    background-color: #e74c3c;
}

/* Responsive styles */
@media (max-width: 768px) {
    .navbar {
        padding: 20px;
    }

    .nav-links {
        display: none; /* Hide navigation links by default */
        flex-direction: column;
        position: absolute;
        top: 70px; /* Adjust the top position */
        left: 0;
        width: 100%;
        background-color: white;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        z-index: 1000;
        padding: 20px; /* Adjust padding */
        border-radius: 15px; /* Add rounded corners */
    }

    .nav-links.active {
        display: flex; /* Show navigation links when active */
    }

    .hamburger-icon {
        display: block; /* Show the hamburger icon on small screens */
        position: relative;
        z-index: 1001; /* Ensure it's above the nav links */
    }
}

 
/* NavbarStyles.css */

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    margin-right: 20px;
    display: none; /* Initially hidden on larger screens */
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7;
    border-radius: 25px;
    padding: 10px 20px;
    text-align: center;
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

.nav-link a {
    color: #2d3436; /* Set link color */
    text-decoration: none; /* Remove underline */
    transition: color 0.3s ease-in-out;
}

.nav-link:hover {
    background-color: #fdcb6e; /* Hover background color */
    transform: scale(1.05); /* Slightly increase size on hover */
}

.nav-link a:hover {
    color: black; /* Hover link color */
}

/* Apply styles to the active link */
.nav-link.active {
    background-color: #e74c3c;
}

/* Responsive styles */
@media (max-width: 768px) {
    .navbar {
        padding: 20px;
    }

    .nav-links {
        display: none; /* Hide navigation links by default */
        flex-direction: column;
        position: absolute;
        top: 70px; /* Adjust the top position */
        left: 0;
        width: 100%;
        background-color: white;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }

    .nav-links.active {
        display: flex; /* Show navigation links when active */
    }

    .hamburger-icon {
        display: block; /* Show the hamburger icon on small screens */
    }
}

 
/* NavbarStyles.css */

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    margin-right: 20px;
    display: none; /* Initially hidden on larger screens */
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7;
    border-radius: 25px;
    padding: 10px 20px;
    text-align: center;
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

.nav-link a {
    color: #2d3436; /* Set link color */
    text-decoration: none; /* Remove underline */
    transition: color 0.3s ease-in-out;
}

.nav-link:hover {
    background-color: #fdcb6e; /* Hover background color */
    transform: scale(1.05); /* Slightly increase size on hover */
}

.nav-link a:hover {
    color: black; /* Hover link color */
}

/* Apply styles to the active link */
.nav-link.active {
    background-color: #e74c3c;
}

/* Responsive styles */
@media (max-width: 768px) {
    .navbar {
        padding: 20px;
    }

    .nav-links {
        display: none; /* Hide navigation links by default */
        flex-direction: column;
        position: absolute;
        top: 70px; /* Adjust the top position */
        left: 0;
        width: 100%;
        background-color: white;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }

    .nav-links.active {
        display: flex; /* Show navigation links when active */
    }

    .hamburger-icon {
        display: block; /* Show the hamburger icon on small screens */
    }
}

 
/* NavbarStyles.css */

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    margin-right: 20px;
    display: none; /* Initially hidden on larger screens */
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7;
    border-radius: 25px;
    padding: 10px 20px;
    text-align: center;
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

.nav-link a {
    color: #2d3436; /* Set link color */
    text-decoration: none; /* Remove underline */
    transition: color 0.3s ease-in-out;
}

.nav-link:hover {
    background-color: #fdcb6e; /* Hover background color */
    transform: scale(1.05); /* Slightly increase size on hover */
}

.nav-link a:hover {
    color: black; /* Hover link color */
}

/* Apply styles to the active link */
.nav-link.active {
    background-color: #e74c3c;
}

/* Responsive styles */
@media (max-width: 768px) {
    .navbar {
        padding: 20px;
    }

    .nav-links {
        display: none; /* Hide navigation links by default */
        flex-direction: column;
        position: absolute;
        top: 70px; /* Adjust the top position */
        left: 0;
        width: 100%;
        background-color: white;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }

    .nav-links.active {
        display: flex; /* Show navigation links when active */
    }

    .hamburger-icon {
        display: block; /* Show the hamburger icon on small screens */
    }
}

 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    margin-right: 20px;
    display: none; /* Initially hidden on larger screens */
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7;
    border-radius: 25px;
    padding: 10px 20px;
    text-align: center;
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

/* ... Other styles ... */

/* Responsive styles */
@media (max-width: 768px) {
    .navbar {
        padding: 20px;
    }

    .nav-links {
        display: none; /* Hide navigation links by default */
        flex-direction: column;
        position: absolute;
        top: 70px; /* Adjust the top position */
        left: 0;
        width: 100%;
        background-color: white;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }

    .nav-links.active {
        display: flex; /* Show navigation links when active */
    }

    .hamburger-icon {
        display: block; /* Show the hamburger icon on small screens */
    }
}

 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.hamburger-icon {
    font-size: 24px;
    cursor: pointer;
    margin-left: 10px;
    display: none; /* Initially hidden on larger screens */
}

.nav-links-container {
    position: fixed;
    top: 0;
    left: -100%; /* Initially hidden off the screen */
    width: 70%;
    max-width: 300px; /* Set a maximum width for the links container */
    height: 100vh;
    background-color: white;
    box-shadow: 2px 0px 6px rgba(0, 0, 0, 0.2); /* Shadow effect */
    transition: left 0.3s ease-in-out;
    z-index: 1000;
}

.nav-links {
    list-style: none;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    margin: 40px 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7;
    border-radius: 25px;
    padding: 10px 20px;
    text-align: center;
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

/* ... Other styles ... */

/* Responsive styles */
@media (max-width: 768px) {
    .navbar {
        padding: 20px;
    }

    .nav-links-container {
        left: 0; /* Show the links container on click */
    }

    .hamburger-icon {
        display: block; /* Show the hamburger icon on small screens */
    }
}

 
import React, { useState } from 'react';
import { NavLink } from 'react-router-dom';
import { FaBars, FaTimes } from 'react-icons/fa';
import './NavbarStyles.css';
import logo from '../assets/RAC-logo.png';

const Navbar = () => {
  const [showLinks, setShowLinks] = useState(false);

  const toggleLinks = () => {
    setShowLinks(!showLinks);
  };

  return (
    <nav className="navbar">
      <div className="logo-container">
        <img src={logo} alt="Logo" className="logo" />
      </div>
      <div className="hamburger-icon" onClick={toggleLinks}>
        {showLinks ? <FaTimes /> : <FaBars />}
      </div>
      <div
        className={`nav-links-container ${showLinks ? 'show' : ''}`}
        onClick={toggleLinks}
      >
        <ul className="nav-links">
          <li className="nav-link">
            <NavLink to="/" exact activeClassName="active">
              Home
            </NavLink>
          </li>
          <li className="nav-link">
            <NavLink to="/about" activeClassName="active">
              About
            </NavLink>
          </li>
          <li className="nav-link">
            <NavLink to="/ourteam" activeClassName="active">
              Our Team
            </NavLink>
          </li>
          <li className="nav-link">
            <NavLink to="/projects" activeClassName="active">
              Projects
            </NavLink>
          </li>
          <li className="nav-link">
            <NavLink to="/alumni" activeClassName="active">
              Alumni
            </NavLink>
          </li>
          <li className="nav-link">
            <NavLink to="/workshops" activeClassName="active">
              Workshops
            </NavLink>
          </li>
        </ul>
      </div>
    </nav>
  );
};

export default Navbar;

 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px; /* Reduce the gap between nav-links */
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7; 
    border-radius: 25px;
    padding: 10px 20px; /* Adjust padding */
    text-align: center; /* Center text within the box */
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

.nav-link a {
    color: #2d3436; /* Set link color */
    text-decoration: none;
    transition: color 0.3s ease-in-out;
}

.nav-link:hover {
    background-color: #fdcb6e; /* Hover background color */
    transform: scale(1.05); /* Slightly increase size on hover */
}

.nav-link a:hover {
    color: black; /* Hover link color */
}

/* Apply styles to the active link */
.nav-link.active {
    background-color: #e74c3c;
}



@media (max-width: 768px) {
    /* Responsive styles for devices with width up to 768px */
    .navbar {
        flex-direction: column;
        align-items: center;
        padding: 20px;
    }

    .nav-links {
        margin-top: 20px;
        flex-direction: column;
    }

    .nav-link {
        width: 100%;
        margin-bottom: 10px;
    }

    
}


 
import { NavLink } from 'react-router-dom';
import './NavbarStyles.css'; 
import logo from '../assets/RAC-logo.png'
import { useState } from 'react';

const Navbar = () => {
 

  return (
    
    <nav className="navbar">
      <div className="logo-container">
        <img src={logo} alt="Logo" className="logo" />
      </div>
      <ul className="nav-links">
        <li className="nav-link">
          <NavLink to="/" exact activeClassName="active">Home</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/about" activeClassName="active">About</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/ourteam" activeClassName="active">Our Team</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/projects" activeClassName="active">Projects</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/alumni" activeClassName="active">Alumni</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/workshops" activeClassName="active">Workshops</NavLink>
        </li>
      </ul>
    
    </nav>
    
    
    
  );
};

export default Navbar;
 
 const[click,setClick]=useState(false);
  const handleClick=()=>setClick(!click);
 
.hamburger{
    display: none;
}
 
.hamburger{
        display: initial;
    }
 
<div className="hamburger" onClick={handleClick}>
                    {click?(
                    <FaTimes size={20} style={{color:"blue"}}/>
                    ):(
                    <FaBars size={20} style={{color:"blue"}}/>
                    )}

                </div>
    
 
</div>
 
  <div className="hamburger" onClick={handleClick}>
                    {click?(
                    <FaTimes size={20} style={{color:"blue"}}/>
                    ):(
                    <FaBars size={20} style={{color:"blue"}}/>
                    )}

                </div>
 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
    background-color: white; /* Set background color */
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
}

.logo {
    width: 80px;
    height: auto;
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px; /* Reduce the gap between nav-links */
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #ffeaa7; /* Darken the background color */
    border-radius: 25px;
    padding: 10px 20px; /* Adjust padding */
    text-align: center; /* Center text within the box */
    transition: background-color 0.3s ease-in-out, transform 0.2s ease-in-out;
    transform-origin: center;
}

.nav-link a {
    color: #2d3436; /* Set link color */
    text-decoration: none;
    transition: color 0.3s ease-in-out;
}

.nav-link:hover {
    background-color: #fdcb6e; /* Hover background color */
    transform: scale(1.05); /* Slightly increase size on hover */
}

.nav-link a:hover {
    color: black; /* Hover link color */
}

/* Apply styles to the active link */
.nav-link.active {
    background-color: #e74c3c;
}

@media (max-width: 768px) {
    /* Responsive styles for devices with width up to 768px */
    .navbar {
        flex-direction: column;
        align-items: center;
        padding: 20px;
    }

    .nav-links {
        margin-top: 20px;
        flex-direction: column;
    }

    .nav-link {
        width: 100%;
        margin-bottom: 10px;
    }
}

 
import { NavLink } from 'react-router-dom';
import './NavbarStyles.css'; 
import logo from '../assets/RAC-logo.png'

const Navbar = () => {
  return (
    <nav className="navbar">
      <div className="logo-container">
        <img src={logo} alt="Logo" className="logo" />
      </div>
      <ul className="nav-links">
        <li className="nav-link">
          <NavLink to="/" exact activeClassName="active">Home</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/about" activeClassName="active">About</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/ourteam" activeClassName="active">Our Team</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/projects" activeClassName="active">Projects</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/alumni" activeClassName="active">Alumni</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/workshops" activeClassName="active">Workshops</NavLink>
        </li>
      </ul>
    </nav>
  );
};

export default Navbar;
 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 90px; /* Adjust horizontal padding */
}

.logo {
    width: 100px;
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px; /* Reduce the gap between nav-links */
    margin: 0;
    padding: 0;
}

.nav-link {
    background-color: #fdcb6e;
    border-radius: 20px;
    padding: 6px 10px; /* Adjust padding */
    width: 100px; /* Adjust width */
    text-align: center; /* Center text within the box */
    transition: background-color 0.3s ease-in-out;
}

.nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s ease-in-out;
}

.nav-link:hover {
    background-color: #2980b9;
}

.nav-link a:hover {
    color: #fff;
}

/* .active {
    background-color: #2ecc71;
}
*/

@media (max-width: 768px) {
    /* Responsive styles for devices with width up to 768px */
    .navbar {
        flex-direction: column;
        align-items: flex-start;
    }

    .nav-links {
        margin-top: 20px;
        flex-direction: column;
    }

    .nav-link {
        width: 100%;
        margin-bottom: 10px;
    }
}

 
import { NavLink } from 'react-router-dom';
import './NavbarStyles.css'; 
import logo from '../assets/RAC-logo.png'

const Navbar = () => {
  return (
    <nav className="navbar">
      <div className="logo-container">
        <img src={logo} alt="Logo" className="logo" />
      </div>
      <ul className="nav-links">
        <li className="nav-link">
          <NavLink to="/" exact activeClassName="active">Home</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/about" activeClassName="active">About</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/ourteam" activeClassName="active">Our Team</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/projects" activeClassName="active">Projects</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/alumni" activeClassName="active">Alumni</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/workshops" activeClassName="active">Workshops</NavLink>
        </li>
      </ul>
    </nav>
  );
};

export default Navbar;
 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 40px; /* Increase the horizontal padding */
  }
  
  .logo {
    width: 100px;
  }
  
  .nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    background-color: #fdcb6e;
    border-radius: 20px;
    padding: 10px 20px;
    transition: background-color 0.3s ease-in-out;
  }
  
  .nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s ease-in-out;
  }
  
  .nav-link:hover {
    background-color: #2980b9;
  }
  
  .nav-link a:hover {
    color: #fff;
  }
  
  /* .active {
    background-color: #2ecc71;
  }
  */
  
  @media (max-width: 768px) {
    /* Responsive styles for devices with width up to 768px */
    .navbar {
      flex-direction: column;
      align-items: flex-start;
    }
  
    .nav-links {
      margin-top: 20px;
      flex-direction: column;
    }
  
    .nav-link {
      width: 100%;
      margin-bottom: 10px;
    }
  }

 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
  }
  
  .logo {
    width: 100px;
  }
  
  .nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    background-color: #fdcb6e;
    border-radius: 20px;
    padding: 10px 20px;
    transition: background-color 0.3s ease-in-out;
  }
  
  .nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s ease-in-out;
  }
  
  .nav-link:hover {
    background-color: #2980b9;
  }
  
  .nav-link a:hover {
    color: #fff;
  }
  
  /* .active {
    background-color: #2ecc71;
  }
  */
  
  @media (max-width: 768px) {
    /* Responsive styles for devices with width up to 768px */
    .navbar {
      flex-direction: column;
      align-items: flex-start;
    }
  
    .nav-links {
      margin-top: 20px;
      flex-direction: column;
    }
  
    .nav-link {
      width: 100%;
      margin-bottom: 10px;
    }
  }
  
 
/* NavbarStyles.css */

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #fdcb6e;
    padding: 10px 30px;
  }
  
  .logo-container {
    flex: 1;
    text-align: left;
  }
  
  .logo {
    max-width: 100px;
  }
  
  .nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    border-radius: 20px;
    background-color: #fdcb6e;
    padding: 8px 16px;
  }
  
  .nav-link a {
    text-decoration: none;
    color: #333; /* Change link color if needed */
    transition: background-color 0.3s ease;
  }
  
  .nav-link a:hover {
    background-color: #e0b93b; /* Change hover color if needed */
  }
  
  @media (max-width: 768px) {
    .navbar {
      flex-direction: column;
      align-items: flex-start;
    }
  
    .nav-links {
      flex-direction: column;
      gap: 10px;
      margin-top: 10px;
    }
  }
  
 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
  }
  
  .logo {
    width: 100px;
  }
  
  .nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    background-color: #fdcb6e;
    border-radius: 20px;
    padding: 10px 20px;
    transition: background-color 0.3s ease-in-out;
  }
  
  .nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s ease-in-out;
  }
  
  .nav-link:hover {
    background-color: #2980b9;
  }
  
  .nav-link a:hover {
    color: #fff;
  }
  
  /* .active {
    background-color: #2ecc71;
  }
  */
  
  @media (max-width: 768px) {
    /* Responsive styles for devices with width up to 768px */
    .navbar {
      flex-direction: column;
      align-items: flex-start;
    }
  
    .nav-links {
      margin-top: 20px;
      flex-direction: column;
    }
  
    .nav-link {
      width: 100%;
      margin-bottom: 10px;
    }
  }
  
 
import React, { useState } from 'react';
import { NavLink } from 'react-router-dom';
import './NavbarStyles.css';
import logo from '../assets/RAC-logo.png';

const Navbar = () => {
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);

  const toggleMobileMenu = () => {
    setIsMobileMenuOpen(!isMobileMenuOpen);
  };

  return (
    <nav className="navbar">
      <div className="logo-container">
        <img src={logo} alt="Logo" className="logo" />
      </div>
      <div className={`mobile-menu-icon ${isMobileMenuOpen ? 'open' : ''}`} onClick={toggleMobileMenu}>
        <div className="bar"></div>
        <div className="bar"></div>
        <div className="bar"></div>
      </div>
      <ul className={`nav-links ${isMobileMenuOpen ? 'mobile-open' : ''}`}>
        <li className="nav-link">
          <NavLink to="/" exact activeClassName="active">Home</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/about" activeClassName="active">About</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/ourteam" activeClassName="active">Our Team</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/projects" activeClassName="active">Projects</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/alumni" activeClassName="active">Alumni</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/workshops" activeClassName="active">Workshops</NavLink>
        </li>
      </ul>
    </nav>
  );
};

export default Navbar;

 
import React, { useState } from 'react';
import { NavLink } from 'react-router-dom';
import './NavbarStyles.css';
import logo from '../assets/RAC-logo.png';

const Navbar = () => {
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);

  const toggleMobileMenu = () => {
    setIsMobileMenuOpen(!isMobileMenuOpen);
  };

  return (
    <nav className="navbar">
      <div className="logo-container">
        <img src={logo} alt="Logo" className="logo" />
      </div>
      <div className={`mobile-menu-icon ${isMobileMenuOpen ? 'open' : ''}`} onClick={toggleMobileMenu}>
        <div className="bar"></div>
        <div className="bar"></div>
        <div className="bar"></div>
      </div>
      <ul className={`nav-links ${isMobileMenuOpen ? 'mobile-open' : ''}`}>
        <li className="nav-link">
          <NavLink to="/" exact activeClassName="active">Home</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/about" activeClassName="active">About</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/ourteam" activeClassName="active">Our Team</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/projects" activeClassName="active">Projects</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/alumni" activeClassName="active">Alumni</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/workshops" activeClassName="active">Workshops</NavLink>
        </li>
      </ul>
    </nav>
  );
};

export default Navbar;

 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
  }
  
  .logo {
    width: 100px;
  }
  
  .nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
  }
  
  .nav-link {
    background-color: #fdcb6e;
    border-radius: 20px;
    padding: 10px 20px;
    transition: background-color 0.3s ease-in-out;
  }
  
  .nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s ease-in-out;
  }
  
  .nav-link:hover {
    background-color: #2980b9;
  }
  
  .nav-link a:hover {
    color: #fff;
  }
  
  /* .active {
    background-color: #2ecc71;
  }
  */
  
  @media (max-width: 768px) {
    /* Responsive styles for devices with width up to 768px */
    .navbar {
      flex-direction: column;
      align-items: flex-start;
    }
  
    .nav-links {
      margin-top: 20px;
      flex-direction: column;
    }
  
    .nav-link {
      width: 100%;
      margin-bottom: 10px;
    }
  }
  
 
import React from 'react';
import { NavLink } from 'react-router-dom';
import './NavbarStyles.css'; 
import logo from '../assets/RAC-logo.png'

const Navbar = () => {
  return (
    <nav className="navbar">
      <div className="logo-container">
        <img src={logo} alt="Logo" className="logo" />
      </div>
      <ul className="nav-links">
        <li className="nav-link">
          <NavLink to="/" exact activeClassName="active">Home</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/about" activeClassName="active">About</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/ourteam" activeClassName="active">Our Team</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/projects" activeClassName="active">Projects</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/alumni" activeClassName="active">Alumni</NavLink>
        </li>
        <li className="nav-link">
          <NavLink to="/workshops" activeClassName="active">Workshops</NavLink>
        </li>
      </ul>
    </nav>
  );
};

export default Navbar;

 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
  }
  
  .logo {
    width: 100px;
  }
  
  .nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
  }
  
  .nav-link {
    background-color: #fdcb6e;
    border-radius: 20px;
    padding: 10px 20px;
    transition: background-color 0.3s ease-in-out;
  }
  
  .nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s ease-in-out;
  }
  
  .nav-link:hover {
    background-color: #2980b9;
  }
  
  .nav-link a:hover {
    color: #fff;
  }
  
  /* .active {
    background-color: #2ecc71;
  }
   */
 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
  }
  
  .logo {
    width: 100px;
  }
  
  .nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
  }
  
  .nav-link {
    background-color: #fdcb6e;
    border-radius: 20px;
    padding: 10px 20px;
    transition: background-color 0.3s ease-in-out;
  }
  
  .nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s ease-in-out;
  }
  
  .nav-link:hover {
    background-color: #2980b9;
  }
  
  .nav-link a:hover {
    color: #fff;
  }
  
  /* .active {
    background-color: #2ecc71;
  }
   */
 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
  }
  
  .logo {
    width: 100px;
  }
  
  .nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
  }
  
  .nav-link {
    background-color: #fdcb6e;
    border-radius: 20px;
    padding: 10px 20px;
    transition: background-color 0.3s ease-in-out;
  }
  
  .nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s ease-in-out;
  }
  
  .nav-link:hover {
    background-color: #2980b9;
  }
  
  .nav-link a:hover {
    color: #fff;
  }
  
  /* .active {
    background-color: #2ecc71;
  }
   */
 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
  }
  
  .logo {
    width: 100px;
  }
  
  .nav-links-container {
    background-color: #fdcb6e;
    border-radius: 30px;
    padding: 10px;
  }
  
  .nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
  }
  
  .nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s ease-in-out;
  }
  
  .nav-link:hover {
    background-color: #2980b9;
  }
  
  .nav-link a:hover {
    color: #fff;
  }
  
  .active {
    background-color: #2ecc71;
  }
  
 
/* Navbar.css */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
  }
  
  .logo {
    width: 100px;
  }
  
  .nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
  }
  
  .nav-link {
    background-color: #fdcb6e;
    border-radius: 20px;
    padding: 10px 20px;
    transition: background-color 0.3s ease-in-out;
  }
  
  .nav-link a {
    color: black;
    text-decoration: none;
    transition: color 0.3s ease-in-out;
  }
  
  .nav-link:hover {
    background-color: #2980b9;
  }
  
  .nav-link a:hover {
    color: #fff;
  }
  
  /* .active {
    background-color: #2ecc71;
  }
   */
 
.header{
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem;
    position: fixed;
    width: 100%;
    height: 90px;
    z-index: 10;
}

.header-bg{
    background-color: rgba(0,0,0,0.85); 
   
    transition: 0.5s;
}
.nav-menu{
    display: flex;
}

.nav-menu li{
    padding: 0 1rem;
}

.nav-menu li a{
    font-size: 1.2rem;
    font-weight: 500;
}

.hamburger{
    display: none;
}

@media screen and (max-width:1040px) {
    .nav-menu{
        flex-direction: column;
        justify-content: center;
        align-items: center;
        width: 100%;
        height: 100vh;
        background: rgba(0,0,0,0.9);
        position: absolute;
        top: 0;
        left: -100%;
        z-index: -3;
        transition: 0.3s;

    }

    .nav-menu.active{
        left: 0;
    }

    .nav-menu li{
        padding:1rem 0;
    }

    .nav-menu li a{
        font-size: 2rem;
    }

    .hamburger{
        display: initial;
    }
    
}
 
import "./NavbarStyles.css";
import React from 'react';
import {Link} from "react-router-dom";
import {FaBars, FaTimes} from "react-icons/fa";
import {useState} from "react";

const Navbar=()=>{
    const[click,setClick]=useState(false);
    const handleClick=()=>setClick(!click);
    const[color,setColor]=useState(false);
    const changeColor=()=>{

        if(window.scrollY>=100){
            setColor(true);
        }else{
            setColor(false);
        }
    };

    window.addEventListener("scroll",changeColor);
    return(

        <div className={color?"header header-bg": "header"
        }>
            <Link to="/">
               <h1>Jyotsna Jha</h1> 
                </Link>   
                <ul className={click? "nav-menu active" :" nav-menu"}>
                    <li>
                        <Link to="/">Home</Link>
                    </li>

                    <li>
                        <Link to="/project">Project</Link>
                    </li>

                    <li>
                        <Link to="/about">About</Link>
                    </li>

                    <li>
                        <Link to="/Resume">Resume</Link>
                    </li>

                    <li>
                        <Link to="/contact">Contact</Link>
                    </li>
                </ul>
                <div className="hamburger" onClick={handleClick}>
                    {click?(
                    <FaTimes size={20} style={{color:"#fff"}}/>
                    ):(
                    <FaBars size={20} style={{color:"#fff"}}/>
                    )}

                </div>
        </div>

    );

};

export default Navbar;
 
 <div>
      <h1>I am home page!</h1>
    </div>
 
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


 
